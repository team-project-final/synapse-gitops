# 핸드오프: 2026-06-10 세션 — W3/W4 마감 + 라이브 누적검증 + 후속 정리

> **작성**: 2026-06-10 · **상태**: W1~W5 추적 잔여 0건, 라이브 누적검증 완료 · **OPEN 이슈 4건**(저우선 후속/신규)
> **정본 허브**: synapse-shared `HANDOFF_HUB.md`(team-lead 유지)
> **선행 핸드오프**: `HANDOFF_W5.md`, `HANDOFF_2026-06-09-followups.md`

---

## 1. 이번 세션 작업 요약

W5 마감 이후 ① W3/W4 감사 잔여 마감 ② 라이브 EKS 1회로 누적 5건 검증 ③ #164/#165 후속 착수 ④ CI ruleset 정정.

### 머지된 PR (synapse-gitops)
| PR | 내용 |
|----|------|
| #168 | docs: **W4 마감** — 실도메인 3항목 nip.io 임시완결(#121) + D-043 team-lead 사인오프 → Step9/10 Done |
| #169 | docs(pm): **WORKFLOW W3/W4 현행화** — staging 4/5→5/5·실도메인 nip.io·IU E2E·D-043 |
| #170 | feat(observability): **W3 Step8 잔여 완료** — ESO 알람 rule(`externalsecret_sync_calls_error`)·앱별 대시보드(`grafana-dashboard-apps.yaml`)·Grafana nip.io ingress |
| #171 | fix(bring-up): **#166** `phase_db_init` `\gexec` stdin 전달 정정(`psql -c`가 `\gexec` 미처리 버그) → **#166 close** |
| #172 | fix: **learning-card 안정화(#164)** — 콜드스타트 cpu req 250m·mem 384/768Mi·startupProbe 300s + staging 이미지 `dev-latest`→SHA 핀(결정성) + **#165** PR BEHIND 레이스 완화 |
| #173 | fix(ci): **ruleset required check 컨텍스트** 워크플로명→잡명 `validate` 정정(PR BLOCKED 해소) |

추가: #165 SHA↔semver 태깅 전략 **결정 문서**(`29c336b`, 옵션 a/b/c + 권장 c), deploy bump(knowledge-svc `2948ad3`, learning-card `ab67c3c`).

### 라이브 누적검증 (2026-06-10, dev EKS 1회 → destroy63·과금0)
- **#166 토픽/DB 라이브검증**: `bring-up.sh` phase_kafka_topics(토픽 9종)·phase_db_init(DB 5종) — db-init `\gexec` 버그 발견·수정(PR #171) → **#166 close**.
- **ESO 알람**: `externalsecret_sync_calls_error` rule 발화 확인.
- **앱별 대시보드**: Grafana 로드 확인. (Grafana nip.io 노출은 manifest-ready 상태로 보류)
- **#164 근본원인 규명**: learning-card 느린 기동 vs startupProbe(cpu req 100m 스로틀) + staging `dev-latest` 비결정성 + readiness 401 → 조치 3건(PR #172 + 이슈 코멘트). staging 401은 `synapse-learning-svc#74`로 핸드오프.

---

## 2. OPEN 이슈 4건 (다음 세션 이관)

### #164 — learning-card-staging Degraded 조사 (진행중)
- **조치 완료(PR #172)**: 콜드스타트 안정화(cpu 250m·startupProbe 300s)·staging 이미지 SHA 핀.
- **남은 것**: 라이브 윈도우에서 안정화 효과 재검증 + staging readiness 401 근본원인(앱레벨, `synapse-learning-svc#74`).
- **우선순위**: 낮음(staging 한정, dev 정상).

### #165 — learning-ai·card semver 재핀 (전략 결정 대기)
- **현황**: `deploy-service.yml`(shared) SHA 태깅 ↔ IU `semver` 전략 충돌 → 매 배포 SHA 회귀. 결정 문서(`29c336b`)에 옵션 a(임시 재태그)/b(deploy semver 전환)/c(IU digest 전환) 정리, **권장 c**.
- **다음 액션**: 팀 결정(b/c, 크로스레포 조율) → 일괄 적용.
- **우선순위**: 낮음(런타임 영향 없음, IU 자동업데이트만).

### #174 — ES analysis-nori 플러그인 커스텀 이미지 (신규, 06-10)
- **증상**: knowledge 검색 100% C500 — ES stock `9.2.1`에 `nori_tokenizer` 미설치 → `ensureIndex()` rethrow. **로컬 e2e·EKS 전 환경** 동일(검색이 한 번도 정상 동작한 적 없음). W4 성공기준 #4·SLA P3 측정 불가.
- **변경 지점**: 커스텀 ES 이미지(`FROM ...:9.2.1` + `elasticsearch-plugin install analysis-nori`) → ECR push → `apps/elasticsearch/base/statefulset.yaml:23` TODO(FS-D) 해소. shared docker-compose 병행. 적용 후 `notes-v1` 재인덱싱.
- **DoD**: `_cat/plugins`에 `analysis-nori` · `ensureIndex` 성공 · `/api/v1/notes/search` 200.
- **우선순위**: 중(검색 기능 전면 차단).

### #175 — bringup.out 등 실행 산출물 .gitignore (신규, 06-10)
- **대상**: `bringup.out`·`destroy*.out`·`grafana-curl*.{sh,out}`·`grafana-diag/final*`·`verify164.*` 등 untracked 산출물(현재 작업트리에 다수).
- **다음 액션**: `.gitignore`에 패턴 추가(예: `*.out`, `grafana-*.sh`, `verify*.sh`/`.out`).
- **우선순위**: 낮음(위생).

---

## 3. 레포 상태

- **추적 잔여**: W1~W5 추적 잔여 **0건**. OPEN은 라이브/팀결정 후속(#164·#165) + 신규(#174·#175)만.
- **main HEAD**: `8c065f6`(PR #173 머지) + deploy bumps.
- **CI**: main 보호(PR + required check `validate` — PR #173로 컨텍스트 정정). `validate`/diff-comment/parse.
- **클러스터**: destroy 완료(과금 0). S3 state + DynamoDB lock만 유지.
- **설계/플랜**: `docs/superpowers/specs/2026-06-10-*`, `docs/superpowers/plans/2026-06-10-*`(learning-card 안정화·BEHIND 레이스·SHA↔semver 결정).

### 다음 라이브 윈도우 체크리스트
1. `bash scripts/bring-up.sh --to manifests`
2. 터널: `source scripts/lib/eks-tunnel.sh; tunnel_up`
3. DB 5종 + Kafka 토픽 9종 — **이제 bring-up phase 자동화**(#166, db-init/kafka-topics phase). 산출물 확인.
4. **#164 재검증**: learning-card-staging 안정화 효과 + readiness 401(`learning-svc#74`).
5. **#174 검증**(ES 커스텀 이미지 적용 후): `_cat/plugins` nori + 검색 200.
6. 검증 → `bash scripts/bring-up.sh --destroy`
