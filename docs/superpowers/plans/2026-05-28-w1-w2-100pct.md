# W1/W2 PM 문서 100% 정합화 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** synapse-gitops PM 문서(TASK/WORKFLOW/HISTORY)의 W1/W2 진척률을 사실에 맞춰 100%로 정합화하고, 14건의 미해결 박스를 다음 주차(W3/W4/W5)로 물리적으로 이월한다.

**Architecture:** 단일 브랜치 `chore/w1-w2-100pct-pm-consolidation`에서 작업. 작업 단위는 5개 Phase × 다수 commit. Phase A(사실 반영) / B(신규 작업) / C(이월) 는 독립적이라 순서 무관, Phase D(Status 갱신) 는 A/B/C 종료 후, Phase E(HISTORY+PR) 는 D 종료 후.

**Tech Stack:** Markdown + GitHub Markdown (체크박스 파서) + git + gh CLI. PowerShell 환경. WORKFLOW 파서는 정규식 `^- \[[ x]\]`만 본다.

---

## File Structure

| 파일 | 역할 | 변경 유형 |
|---|---|---|
| `docs/project-management/task/TASK_gitops.md` | Step 단위 정의(Done When + Status) | 수정 (A, C, D 단계) |
| `docs/project-management/workflow/WORKFLOW_gitops_W1.md` | W1 세부 체크리스트 | 수정 (A: 1라인 제거, C: 7라인 제거, 모든 라인 주석화) |
| `docs/project-management/workflow/WORKFLOW_gitops_W2.md` | W2 세부 체크리스트 | 수정 (A: 4라인 ✅, B: 2라인 ✅, C: 7라인 제거+주석) |
| `docs/project-management/workflow/WORKFLOW_gitops_W3.md` | W3 세부 체크리스트 | 수정 (C: Step 8.4에 +1) |
| `docs/project-management/workflow/WORKFLOW_gitops_W4.md` | W4 세부 체크리스트 | 수정 (C: Step 9에 +7, Step 10에 +3) |
| `docs/project-management/workflow/WORKFLOW_gitops_W5.md` | W5 세부 체크리스트 | 수정 (C: Step 11에 +2, Step 12에 +1) |
| `docs/project-management/history/HISTORY_gitops.md` | 의사결정 로그 | 수정 (E: D-041 추가) |
| `docs/runbooks/image-updater-ecr-setup.md` | Image Updater 운영 가이드 | 수정 (B: 자동 sync 비활성화 섹션 추가) |

---

## 카운트 검증 매트릭스 (작업 시작/종료 양쪽에서 비교)

| 파일 | 시작 시점 `- [ ]` | 종료 시점 `- [ ]` | 변화 |
|---|---|---|---|
| WORKFLOW_W1 | 8 | 0 | -8 (사실반영 1 + root.yaml 1 + 이월 7 = 9... 8개에 root.yaml은 W1 Step 2.2 라인만, TASK도 별도. 자세한 카운트는 Task 1에서 baseline 측정) |
| WORKFLOW_W2 | 13 | 0 | -13 (사실반영 4 + 신규작업 2 + 이월 7 = 13) |
| WORKFLOW_W3 | 1 | 2 | +1 (ESO 알람) |
| WORKFLOW_W4 | 36 | 46 | +10 (Step 9: +7, Step 10: +3) |
| WORKFLOW_W5 | 38 | 41 | +3 (Step 11: +2, Step 12: +1) |
| TASK 본문 (W1/W2 영역) | 5 | 0 | -5 (사실반영 2 + root.yaml 1 + 이월 2) |

Task 1에서 실측 baseline을 확정한다.

---

## Task 1: Baseline 측정 + 작업 환경 확인

**Files:**
- Verify: `docs/project-management/workflow/WORKFLOW_gitops_W*.md`, `docs/project-management/task/TASK_gitops.md`

- [ ] **Step 1: 작업 브랜치 확인**

Run (PowerShell):
```powershell
git status --short
git branch --show-current
```
Expected: 출력 `chore/w1-w2-100pct-pm-consolidation` (clean working tree).

- [ ] **Step 2: 시작 시점 박스 카운트 측정**

Run:
```powershell
Write-Host "--- WORKFLOW baseline (시작) ---"
foreach ($w in @('W1','W2','W3','W4','W5')) {
  $path = "docs/project-management/workflow/WORKFLOW_gitops_$w.md"
  $unchecked = (Get-Content $path | Select-String -Pattern '^- \[ \]' -SimpleMatch:$false).Count
  $checked   = (Get-Content $path | Select-String -Pattern '^- \[x\]' -SimpleMatch:$false).Count
  Write-Host "$w : unchecked=$unchecked, checked=$checked"
}
Write-Host "--- TASK baseline (시작) ---"
$taskPath = "docs/project-management/task/TASK_gitops.md"
$taskUnchecked = (Get-Content $taskPath | Select-String -Pattern '^\s*- \[ \]' -SimpleMatch:$false).Count
Write-Host "TASK : unchecked=$taskUnchecked"
```
Expected (대략): W1 unchecked=8, W2 unchecked=13, W3 unchecked=1, W4 unchecked=36, W5 unchecked=38, TASK unchecked=5+α(W3+ 본문 박스도 포함하므로 9 전후).

이 수치를 메모해 두고 Task 14의 종료 카운트와 비교한다.

- [ ] **Step 3: Commit baseline 측정 결과(메모)**

Baseline은 별도 파일에 적지 않는다(휘발). 본 단계는 측정만 — commit 없음. 다음 Task로.

---

## Task 2: [Phase A.1] W2 Step 4 — 5/5 Healthy 사실 반영

**Files:**
- Modify: `docs/project-management/task/TASK_gitops.md` (라인 83-84)
- Modify: `docs/project-management/workflow/WORKFLOW_gitops_W2.md` (라인 34-36)

- [ ] **Step 1: TASK_gitops.md W2 Step 4 — platform-svc/learning-ai Done When ✅**

`docs/project-management/task/TASK_gitops.md` 의 W2 Step 4 Done When 부분.

Old:
```markdown
  - [ ] EKS 배포: platform-svc Healthy (앱 코드 수정 필요 — mfa_credentials 테이블)
  - [ ] EKS 배포: learning-ai Healthy (앱 코드 수정 필요 — Python 기동 문제)
```

New:
```markdown
  - [x] EKS 배포: platform-svc Healthy (9차 세션: ExternalSecret 11개 + ConfigMap 3개 + Flyway V28 + AES Base64 32B — PR #40)
  - [x] EKS 배포: learning-ai Healthy (9차 세션: 포트 8000→8090 통일 — PR #38)
```

- [ ] **Step 2: WORKFLOW_gitops_W2.md 1.4 적용+검증 4건 ✅**

`docs/project-management/workflow/WORKFLOW_gitops_W2.md` 의 Step 4 1.4 적용+검증 섹션.

Old (라인 34-37 부근):
```markdown
- [ ] EKS 배포: platform-svc 정상 기동 (앱 코드 수정 필요 — mfa_credentials)
- [ ] EKS 배포: learning-ai 정상 기동 (앱 코드 수정 필요 — Python 기동)
- [ ] /actuator/health 200 응답 (3/5 확인, 2개 미확인)
- [ ] dev 도메인으로 5개 앱 도달 — Ingress 미설정
```

`dev 도메인으로 5개 앱 도달`은 본 Task가 아닌 Task 9 (Phase C.3) 이월 대상. 본 Task에서는 위 3건만 ✅:

New:
```markdown
- [x] EKS 배포: platform-svc 정상 기동 (9차 세션: PR #40 환경변수 14개 + Flyway V28 + AES 키 수정)
- [x] EKS 배포: learning-ai 정상 기동 (9차 세션: 포트 8090 통일 PR #38 자동 해결)
- [x] /actuator/health 200 응답 (5/5 — 9차 세션 5/5 Healthy 달성)
- [ ] dev 도메인으로 5개 앱 도달 — Ingress 미설정
```

(마지막 라인은 Task 9에서 이월 처리)

- [ ] **Step 3: 검증 — 변경된 라인 확인**

Run:
```powershell
Select-String -Path "docs/project-management/workflow/WORKFLOW_gitops_W2.md" -Pattern "platform-svc 정상 기동|learning-ai 정상 기동|/actuator/health 200" | Select-Object LineNumber,Line
```
Expected: 3개 라인 모두 `- [x]` 시작 + 9차 세션 메모 포함.

- [ ] **Step 4: Commit**

```powershell
git add docs/project-management/task/TASK_gitops.md docs/project-management/workflow/WORKFLOW_gitops_W2.md
git commit -m @'
docs(pm): W2 Step 4 5/5 Healthy 사실 반영 (Phase A.1)

- TASK Done When: platform-svc / learning-ai → [x]
- WORKFLOW 1.4: platform-svc / learning-ai / /actuator/health 200 → [x]
- 근거: HANDOFF_W2 §2, PR #38 (learning-ai 포트), PR #40 (platform-svc 환경변수 + Flyway V28)

Refs: D-041
Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
'@
```

---

## Task 3: [Phase A.2] W2 Step 5 — gitleaks 정합화

**Files:**
- Modify: `docs/project-management/workflow/WORKFLOW_gitops_W2.md` (라인 69)

- [ ] **Step 1: WORKFLOW W2 Step 5 1.4 gitleaks ✅**

Old:
```markdown
- [ ] gitleaks 또는 trufflehog로 git history 스캔 → 0건 — EKS 배포 후
```

New:
```markdown
- [x] gitleaks 또는 trufflehog로 git history 스캔 → 0건 (gitleaks 8.30.1 / 114 commits / 0 leaks, 2026-05-26)
```

(이미 TASK 본문에는 ✅로 적혀있던 항목 — WORKFLOW의 단순 정합화)

- [ ] **Step 2: 검증**

Run:
```powershell
Select-String -Path "docs/project-management/workflow/WORKFLOW_gitops_W2.md" -Pattern "gitleaks 또는 trufflehog"
```
Expected: 1라인, `- [x]` 시작.

- [ ] **Step 3: Commit**

```powershell
git add docs/project-management/workflow/WORKFLOW_gitops_W2.md
git commit -m @'
docs(pm): W2 Step 5 gitleaks 0건 정합 (Phase A.2)

- WORKFLOW 1.4: gitleaks → [x]
- TASK 본문에는 이미 ✅, WORKFLOW 단순 정합화
- 근거: gitleaks 8.30.1 / 114 commits / 0 leaks (TASK_gitops.md W2 Step 5 본문)

Refs: D-041
Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
'@
```

---

## Task 4: [Phase A.3] W1 Step 2 — `argocd/apps/root.yaml` 선택 항목 정리

**Files:**
- Modify: `docs/project-management/task/TASK_gitops.md` (라인 37)
- Modify: `docs/project-management/workflow/WORKFLOW_gitops_W1.md` (라인 53)

- [ ] **Step 1: TASK_gitops.md W1 Step 2 — root.yaml 라인 제거 + 결정 주석**

Old:
```markdown
  - [ ] `argocd/apps/root.yaml` (app-of-apps) 정의  <!-- 선택 항목, ApplicationSet 단독 운영으로 결정 -->
  - [x] `argocd/applicationset.yaml` 정의 (matrix 5svc × [dev], C3)
```

New:
```markdown
  <!-- 2026-05-28 D-002로 ApplicationSet 단독 채택 — `argocd/apps/root.yaml` (app-of-apps) 항목 제거. PRD FR-GO-103 충족 방식 결정됨. -->
  - [x] `argocd/applicationset.yaml` 정의 (matrix 5svc × [dev], C3)
```

- [ ] **Step 2: WORKFLOW_gitops_W1.md Step 2 1.2 — root.yaml 라인 제거 + 결정 주석**

Old:
```markdown
- [x] argocd/projects.yaml (AppProject 정의, synapse-* namespace 한정)
- [x] argocd/applicationset.yaml (matrix: 5 svc × [dev] = 5 Application, C3)
- [x] 각 app의 source/destination/syncPolicy 템플릿 (`spec.template`)
- [ ] argocd/apps/root.yaml (선택, app-of-apps 진입점)  <!-- 선택 항목, W1 미사용. ApplicationSet 단독 운영 -->
```

New:
```markdown
- [x] argocd/projects.yaml (AppProject 정의, synapse-* namespace 한정)
- [x] argocd/applicationset.yaml (matrix: 5 svc × [dev] = 5 Application, C3)
- [x] 각 app의 source/destination/syncPolicy 템플릿 (`spec.template`)
<!-- 2026-05-28 D-002로 ApplicationSet 단독 채택 — `argocd/apps/root.yaml` 항목 제거 (선택 항목, 채택 안 됨). -->
```

- [ ] **Step 3: 검증 — root.yaml 미체크 박스가 더 이상 없음**

Run:
```powershell
Select-String -Path "docs/project-management/task/TASK_gitops.md","docs/project-management/workflow/WORKFLOW_gitops_W1.md" -Pattern "root\.yaml" | Select-Object Path,LineNumber,Line
```
Expected: HTML 주석 라인 2건만 출력. `- [ ]` 시작 라인 0건.

- [ ] **Step 4: Commit**

```powershell
git add docs/project-management/task/TASK_gitops.md docs/project-management/workflow/WORKFLOW_gitops_W1.md
git commit -m @'
docs(pm): W1 Step 2 root.yaml 선택 항목 정리 (Phase A.3)

- TASK + WORKFLOW에서 root.yaml 미체크 라인 제거
- 자리에 D-002로 채택 안 함 결정 사실 HTML 주석화
- ApplicationSet 단독 운영 채택 (D-002, 2026-05-16)

Refs: D-041, D-002
Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
'@
```

---

## Task 5: [Phase B.1] image-updater 자동 sync 비활성화 절차 추가

**Files:**
- Modify: `docs/runbooks/image-updater-ecr-setup.md` (파일 끝에 새 섹션 추가)
- Modify: `docs/project-management/workflow/WORKFLOW_gitops_W2.md` (라인 100)

- [ ] **Step 1: image-updater-ecr-setup.md 끝에 "긴급 정지 / 자동 sync 비활성화" 섹션 추가**

기존 파일의 마지막 라인 (`B 실행 (prod, W4+)` 섹션) 뒤에 다음을 append:

```markdown

## 긴급 정지 / 자동 sync 비활성화 절차

장애 발생 시 image-updater의 write-back을 차단하거나 ArgoCD 자동 sync를 멈춰야 하는 경우 사용한다. 적용 범위(단일 앱 vs 전체)를 먼저 선택한다.

### 단일 앱만 정지 (가장 빠름, 권장)

```bash
# 1) ArgoCD CLI로 자동 sync + self-heal 끄기
argocd app set <app-name> --auto-prune=false --self-heal=false
argocd app set <app-name> --sync-policy none

# 2) (선택) Image Updater 대상에서 제외 — Application 또는 ApplicationSet annotation 제거
kubectl -n argocd annotate application <app-name> argocd-image-updater.argoproj.io/image-list-
```

### 전체 앱 정지 (ApplicationSet 일시 중단)

`argocd/applicationset.yaml` (또는 `applicationset-staging.yaml`)의 `spec.template.spec.syncPolicy.automated` 블록을 일시 주석 처리 후 git push. main 보호 룰 통과를 위해 PR 머지 필요.

### Image Updater 컨트롤러 자체 정지 (가장 무거운 옵션)

```bash
kubectl -n argocd scale deploy argocd-image-updater --replicas=0
```

ECR 모니터링·write-back이 모두 멈춘다. 컨트롤러 정지 중에도 기존 ArgoCD sync는 계속 동작한다.

### 복귀

위 조치를 역순으로 적용한다. 컨트롤러 재기동 후 다음 polling 주기(기본 2분, ConfigMap `argocd-image-updater-config`의 `polling.interval`로 조정)에 정상 반영.

### 운영 메모

- 본 절차는 dev/staging(A안)에 적용 가능. prod(B안 — PR 기반 write-back)는 PR을 close하면 충분.
- 단일 앱 정지 후 복귀 시 `argocd app sync <app-name>`으로 수동 sync 1회 후 자동 sync 재개를 권장.
```

- [ ] **Step 2: WORKFLOW_gitops_W2.md Step 6 1.4 `자동 sync 비활성화 절차` ✅**

Old:
```markdown
- [ ] 자동 sync 비활성화 절차 (긴급 상황 대비)
```

New:
```markdown
- [x] 자동 sync 비활성화 절차 (긴급 상황 대비) — `docs/runbooks/image-updater-ecr-setup.md` "긴급 정지 / 자동 sync 비활성화 절차" 섹션
```

- [ ] **Step 3: 검증**

Run:
```powershell
Select-String -Path "docs/runbooks/image-updater-ecr-setup.md" -Pattern "긴급 정지|자동 sync 비활성화|argocd app set" | Select-Object LineNumber,Line
```
Expected: 새 섹션이 보임 (헤더 1개 + 본문 다수 라인).

```powershell
Select-String -Path "docs/project-management/workflow/WORKFLOW_gitops_W2.md" -Pattern "자동 sync 비활성화 절차"
```
Expected: `- [x] 자동 sync 비활성화 절차` 시작.

- [ ] **Step 4: Commit**

```powershell
git add docs/runbooks/image-updater-ecr-setup.md docs/project-management/workflow/WORKFLOW_gitops_W2.md
git commit -m @'
docs(runbook): image-updater 긴급 정지/자동 sync 비활성화 절차 (Phase B.1)

- image-updater-ecr-setup.md 끝에 새 섹션 추가:
  - 단일 앱 정지 (argocd app set)
  - 전체 앱 정지 (ApplicationSet 주석)
  - 컨트롤러 정지 (scale --replicas=0)
  - 복귀 + 운영 메모
- WORKFLOW W2 Step 6 1.4 → [x]

Refs: D-041, FR-GO-205
Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
'@
```

---

## Task 6: [Phase B.2] svc팀 배포 흐름 공유 — cross-ref ✅

**Files:**
- Modify: `docs/project-management/workflow/WORKFLOW_gitops_W2.md` (라인 101)

- [ ] **Step 1: 기존 가이드 문서 cross-ref 확인**

Run:
```powershell
Select-String -Path "docs/synapse-developer-guide.md" -Pattern "## 5\. GitOps 배포 흐름"
```
Expected: 1라인, "## 5. GitOps 배포 흐름" (developer-guide §5에 이미 존재).

- [ ] **Step 2: WORKFLOW_gitops_W2.md Step 6 1.4 `svc 팀 배포 흐름 공유` ✅**

Old:
```markdown
- [ ] svc 팀에 새 이미지 푸시 → 배포 흐름 공유
```

New:
```markdown
- [x] svc 팀에 새 이미지 푸시 → 배포 흐름 공유 — `docs/synapse-developer-guide.md` §5 "GitOps 배포 흐름" (PR #28, 808줄 통합 가이드)
```

- [ ] **Step 3: 검증**

Run:
```powershell
Select-String -Path "docs/project-management/workflow/WORKFLOW_gitops_W2.md" -Pattern "svc 팀에 새 이미지 푸시"
```
Expected: `- [x] svc 팀에 새 이미지 푸시 ... developer-guide.md §5 ...` 1라인.

- [ ] **Step 4: Commit**

```powershell
git add docs/project-management/workflow/WORKFLOW_gitops_W2.md
git commit -m @'
docs(pm): W2 Step 6 svc팀 배포 흐름 공유 cross-ref (Phase B.2)

- WORKFLOW Step 6 1.4 → [x] (developer-guide §5 GitOps 배포 흐름 참조)
- 기존 PR #28에 통합 가이드 808줄 — 본 작업은 cross-ref만

Refs: D-041, FR-GO-205
Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
'@
```

---

## Task 7: [Phase C.1] W1 Step 1 4항목 → W4 Step 9 이월

**Files:**
- Modify: `docs/project-management/workflow/WORKFLOW_gitops_W1.md` (라인 21, 22, 28, 29)
- Modify: `docs/project-management/workflow/WORKFLOW_gitops_W4.md` (Step 9 1.1 / 1.3 / 1.4)
- Modify: `docs/project-management/task/TASK_gitops.md` (W1 Step 1 Done When 라인 20 + W4 Step 9 Done When)

- [ ] **Step 1: WORKFLOW_W1 Step 1.2 — ACM/DNS 2건 제거 + 주석화**

Old (`docs/project-management/workflow/WORKFLOW_gitops_W1.md` Step 1.2):
```markdown
- [x] argocd-server Service 외부 노출 (LoadBalancer + AWS NLB annotation)
- [ ] ACM 인증서 ARN 매핑 (HTTPS 종료)  <!-- W2 옵션1 마이그레이션으로 이월 (도메인 미확보) -->
- [ ] DNS 레코드 정의 (argocd.<도메인>)  <!-- W2 옵션1 마이그레이션으로 이월 -->
```

New:
```markdown
- [x] argocd-server Service 외부 노출 (LoadBalancer + AWS NLB annotation)
<!-- 2026-05-28 D-041로 W4 Step 9 (prod 도메인 흐름)로 이월:
     - ACM 인증서 ARN 매핑 (HTTPS 종료)
     - DNS 레코드 정의 (argocd.<도메인>)
     사유: D-001 옵션2(self-signed) 채택 시점에 W2 마이그레이션으로 이월 표시했으나 도메인 미확보. W4 prod 도메인 확보와 함께 처리. -->
```

- [ ] **Step 2: WORKFLOW_W1 Step 1.3 — 외부 도메인 UI/webhook 2건 제거 + 주석화**

Old (Step 1.3):
```markdown
- [x] argocd CLI 로그인 성공 (bootstrap-argocd.sh 5/8)
- [ ] 외부 도메인으로 UI 접속 + TLS 인증서 유효  <!-- W2 옵션1 마이그레이션으로 이월 (NLB DNS + self-signed 사용 중) -->
- [ ] webhook endpoint 외부 도달 (curl 또는 GitHub webhook ping)  <!-- W2 외부 노출 마이그레이션과 함께 이월 -->
```

New:
```markdown
- [x] argocd CLI 로그인 성공 (bootstrap-argocd.sh 5/8)
<!-- 2026-05-28 D-041로 W4 Step 9 (prod 도메인 흐름)로 이월:
     - 외부 도메인으로 UI 접속 + TLS 인증서 유효
     - webhook endpoint 외부 도달 (curl 또는 GitHub webhook ping)
     사유: 도메인 미확보. W4 prod 도메인 확보와 함께 처리. -->
```

- [ ] **Step 3: WORKFLOW_W4 Step 9에 4항목 추가**

`docs/project-management/workflow/WORKFLOW_gitops_W4.md` Step 9 1.1 사전 분석 끝에 4건 추가:

Old (Step 9 1.1):
```markdown
### 1.1 사전 분석
- [ ] prod 클러스터 / 네임스페이스 분리 정책 (별도 클러스터 권장)
- [ ] 승인 게이트 방식 결정 (ArgoCD Manual Sync vs GH Environment Approval)
- [ ] prod 접근 권한 분리 (별도 IAM/RBAC)
- [ ] 변경 관리 절차 (PR 라벨 + 리뷰어 합의)
```

New:
```markdown
### 1.1 사전 분석
- [ ] prod 클러스터 / 네임스페이스 분리 정책 (별도 클러스터 권장)
- [ ] 승인 게이트 방식 결정 (ArgoCD Manual Sync vs GH Environment Approval)
- [ ] prod 접근 권한 분리 (별도 IAM/RBAC)
- [ ] 변경 관리 절차 (PR 라벨 + 리뷰어 합의)
- [ ] ACM 인증서 ARN 매핑 (HTTPS 종료) — W1 Step 1.2에서 이월 (D-041)
- [ ] DNS 레코드 정의 (argocd.<도메인> + prod 앱 도메인) — W1 Step 1.2에서 이월 (D-041)
- [ ] 외부 도메인으로 ArgoCD UI 접속 + TLS 인증서 유효 — W1 Step 1.3에서 이월 (D-041)
- [ ] webhook endpoint 외부 도달 (curl 또는 GitHub webhook ping) — W1 Step 1.3에서 이월 (D-041)
```

- [ ] **Step 4: TASK_gitops.md W1 Step 1 Done When `webhook endpoint` 라인 제거 + 주석화**

Old (W1 Step 1 Done When):
```markdown
  - [x] admin 비밀번호 회전 + AWS Secrets Manager 저장
  - [x] CLI 로그인 성공
  - [ ] webhook endpoint 외부 도달 확인  <!-- W2 옵션1 마이그레이션과 함께 이월 -->
```

New:
```markdown
  - [x] admin 비밀번호 회전 + AWS Secrets Manager 저장
  - [x] CLI 로그인 성공
  <!-- 2026-05-28 D-041로 W4 Step 9 (prod 도메인 흐름)로 이월: webhook endpoint 외부 도달 확인 + ACM/DNS/외부 TLS (4항목) -->
```

- [ ] **Step 5: TASK_gitops.md W4 Step 9 Done When에 4항목 추가**

Old (W4 Step 9 Done When):
```markdown
- **Done When**:
  - [ ] `apps/{app}/overlays/prod/kustomization.yaml` 5개 작성
  - [ ] ArgoCD AppProject `prod`에 Manual Sync 정책
  - [ ] prod sync 권한이 별도 그룹/사용자에게만 부여
  - [ ] PR-merge → staging 자동 sync → prod 수동 승인 흐름 검증
```

New:
```markdown
- **Done When**:
  - [ ] `apps/{app}/overlays/prod/kustomization.yaml` 5개 작성
  - [ ] ArgoCD AppProject `prod`에 Manual Sync 정책
  - [ ] prod sync 권한이 별도 그룹/사용자에게만 부여
  - [ ] PR-merge → staging 자동 sync → prod 수동 승인 흐름 검증
  - [ ] ACM 인증서 ARN 매핑 + DNS 레코드 (argocd + 5앱 prod 도메인) — W1 이월 (D-041)
  - [ ] 외부 도메인으로 ArgoCD UI HTTPS 접속 + TLS valid — W1 이월 (D-041)
  - [ ] webhook endpoint 외부 도달 확인 — W1 이월 (D-041)
```

(3건만 추가하여 TASK 차원에서는 4항목을 3개 박스로 묶음 — WORKFLOW에선 4개로 분리. 이유: TASK Done When은 결과 기준, WORKFLOW는 세부 단계 기준)

- [ ] **Step 6: 검증**

Run:
```powershell
Write-Host "--- W1 ACM/DNS/webhook/외부TLS 잔존 미체크 확인 (0이어야 함) ---"
Select-String -Path "docs/project-management/workflow/WORKFLOW_gitops_W1.md","docs/project-management/task/TASK_gitops.md" -Pattern '^\s*- \[ \].*(ACM 인증서|DNS 레코드|외부 도메인|webhook endpoint)' | Measure-Object | Select-Object Count

Write-Host "--- W4 Step 9에 이월 박스 4건(WORKFLOW) + 3건(TASK) 신규 ---"
Select-String -Path "docs/project-management/workflow/WORKFLOW_gitops_W4.md" -Pattern 'W1 Step|W1 이월' | Measure-Object | Select-Object Count
Select-String -Path "docs/project-management/task/TASK_gitops.md" -Pattern 'W1 이월' | Measure-Object | Select-Object Count
```
Expected: 첫 카운트 = 0. 둘째 = 4. 셋째 = 3.

- [ ] **Step 7: Commit**

```powershell
git add docs/project-management/workflow/WORKFLOW_gitops_W1.md docs/project-management/workflow/WORKFLOW_gitops_W4.md docs/project-management/task/TASK_gitops.md
git commit -m @'
docs(pm): W1 Step 1 4항목 → W4 Step 9 이월 (Phase C.1)

- WORKFLOW W1 1.2: ACM 인증서, DNS 레코드 → 주석화 + W4 Step 9 1.1에 추가
- WORKFLOW W1 1.3: 외부 도메인 UI/TLS, webhook → 주석화 + W4 Step 9 1.1에 추가
- TASK W1 Step 1 Done When: webhook → 주석화 + W4 Step 9 Done When에 3건 묶어 추가

사유: D-001 옵션2(self-signed) 채택, 도메인 미확보. W4 prod 도메인 흐름과 함께 처리.

Refs: D-041, D-001
Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
'@
```

---

## Task 8: [Phase C.2] W1 Step 3 3항목 → W5 Step 11/12 이월

**Files:**
- Modify: `docs/project-management/workflow/WORKFLOW_gitops_W1.md` (라인 75, 81, 82)
- Modify: `docs/project-management/workflow/WORKFLOW_gitops_W5.md` (Step 11 1.x, Step 12 1.x)
- Modify: `docs/project-management/task/TASK_gitops.md` (W1 Step 3 라인 59 + W5 Step 11/12 Done When)

- [ ] **Step 1: WORKFLOW_W1 Step 3.1 — PR diff 코멘트 도구 후보 제거 + 주석화**

Old (Step 3.1):
```markdown
- [x] 추가 검증 도구 후보 비교 (kubeconform vs kubeval — kubeconform 채택, D-005)
- [ ] PR 영향 범위(diff) 코멘트 도구 후보 (Atlantis, kustomize-diff GH action 등)  <!-- W3 이월 (선택 항목) -->
- [x] branch protection 룰 적용 범위 결정 (필수 status check + REVIEWS 토글, scripts/setup-branch-protection.sh)
```

New:
```markdown
- [x] 추가 검증 도구 후보 비교 (kubeconform vs kubeval — kubeconform 채택, D-005)
<!-- 2026-05-28 D-041로 W5 Step 11 (Runbook + 안정화)로 이월: PR 영향 범위(diff) 코멘트 도구 후보. 사유: 선택 항목, W3 이월 표시 후 W3에서 미진행. -->
- [x] branch protection 룰 적용 범위 결정 (필수 status check + REVIEWS 토글, scripts/setup-branch-protection.sh)
```

- [ ] **Step 2: WORKFLOW_W1 Step 3.2 — kustomize 캐싱 + PR diff 액션 제거 + 주석화**

Old (Step 3.2):
```markdown
- [x] kubeconform 단계 추가 (CRD-catalog schema 포함, `-strict -ignore-missing-schemas`)
- [ ] kustomize build 결과 캐싱 (속도 개선, 선택)  <!-- 선택, W3 성능 개선 시 검토 -->
- [ ] PR diff 코멘트 액션 추가 (선택)  <!-- 선택, W3 이월 -->
```

New:
```markdown
- [x] kubeconform 단계 추가 (CRD-catalog schema 포함, `-strict -ignore-missing-schemas`)
<!-- 2026-05-28 D-041로 이월:
     - kustomize build 결과 캐싱 (속도 개선) → W5 Step 12 (Cost 최적화 + 안정화)
     - PR diff 코멘트 액션 추가 → W5 Step 11 (Runbook + 안정화)
     사유: 선택 항목, W3 이월 표시 후 미진행. -->
```

- [ ] **Step 3: WORKFLOW_W5 Step 11에 2항목 추가**

Old (Step 11 1.1 사전 분석 끝):
```markdown
- [ ] 자주 발생할 장애 유형 도출 (Pod CrashLoop, OOM, sync 실패, 인증서 만료, DB 연결 실패)
- [ ] 각 장애의 신호(symptom) → 원인(cause) → 조치(action) 매핑
- [ ] On-call 로테이션 합의 (W5 이후 운영)
- [ ] 에스컬레이션 기준 (level 1 / 2 / 3)
```

New:
```markdown
- [ ] 자주 발생할 장애 유형 도출 (Pod CrashLoop, OOM, sync 실패, 인증서 만료, DB 연결 실패)
- [ ] 각 장애의 신호(symptom) → 원인(cause) → 조치(action) 매핑
- [ ] On-call 로테이션 합의 (W5 이후 운영)
- [ ] 에스컬레이션 기준 (level 1 / 2 / 3)
- [ ] PR 영향 범위(diff) 코멘트 도구 후보 비교 (Atlantis, kustomize-diff GH action) — W1 Step 3.1에서 이월 (D-041, 선택)
- [ ] PR diff 코멘트 GitHub Action 도입 — W1 Step 3.2에서 이월 (D-041, 선택)
```

- [ ] **Step 4: WORKFLOW_W5 Step 12에 1항목 추가**

Old (Step 12 1.3 안정화 끝):
```markdown
- [ ] W1~W4 잔여 P1 이슈 목록 점검 + 처리
- [ ] 전체 환경 (dev/staging/prod) 헬스체크 통과 확인
- [ ] CI/CD 평균 실행 시간 점검 (회귀 없는지)
- [ ] 알람 false-positive 비율 점검 + 룰 조정
```

New:
```markdown
- [ ] W1~W4 잔여 P1 이슈 목록 점검 + 처리
- [ ] 전체 환경 (dev/staging/prod) 헬스체크 통과 확인
- [ ] CI/CD 평균 실행 시간 점검 (회귀 없는지)
- [ ] 알람 false-positive 비율 점검 + 룰 조정
- [ ] kustomize build 결과 캐싱 (CI 속도 개선) — W1 Step 3.2에서 이월 (D-041, 선택)
```

- [ ] **Step 5: TASK_gitops.md W1 Step 3 Done When `PR 코멘트로 diff 요약` 제거 + 주석화**

Old (W1 Step 3 Done When):
```markdown
  - [x] kubeconform 추가 (Kubernetes 스키마 + CRD 카탈로그 검증)
  - [x] yamllint 룰 보강 (`.yamllint`: line-length 160, indentation 2)
  - [ ] PR 코멘트로 diff 요약 (선택)  <!-- W3 이월 (선택 항목) -->
  - [x] CI 실패 시 머지 차단 (scripts/setup-branch-protection.sh, Task 13)
```

New:
```markdown
  - [x] kubeconform 추가 (Kubernetes 스키마 + CRD 카탈로그 검증)
  - [x] yamllint 룰 보강 (`.yamllint`: line-length 160, indentation 2)
  <!-- 2026-05-28 D-041로 W5 Step 11/12로 이월: PR 코멘트로 diff 요약 (선택) — W3 이월 표시 후 미진행. -->
  - [x] CI 실패 시 머지 차단 (scripts/setup-branch-protection.sh, Task 13)
```

- [ ] **Step 6: TASK_gitops.md W5 Step 11/12 Done When에 항목 추가**

Old (W5 Step 11 Done When):
```markdown
- **Done When**:
  - [ ] 장애 시나리오 5개 이상 Runbook 작성 (Pod CrashLoop, OOM, sync 실패, 인증서 만료, DB 연결 실패)
  - [ ] 각 시나리오에 단계별 진단/조치/에스컬레이션 기준
  - [ ] team-lead가 Runbook 따라하기 1회 검증
  - [ ] On-call 연락처/Slack 채널 정리
```

New:
```markdown
- **Done When**:
  - [ ] 장애 시나리오 5개 이상 Runbook 작성 (Pod CrashLoop, OOM, sync 실패, 인증서 만료, DB 연결 실패)
  - [ ] 각 시나리오에 단계별 진단/조치/에스컬레이션 기준
  - [ ] team-lead가 Runbook 따라하기 1회 검증
  - [ ] On-call 연락처/Slack 채널 정리
  - [ ] PR 코멘트로 diff 요약 GitHub Action 도입 (선택) — W1 이월 (D-041)
```

Old (W5 Step 12 Done When):
```markdown
- **Done When**:
  - [ ] AWS Cost Explorer 태그 기반 비용 가시성 확보
  - [ ] Resource request/limit 적정성 1회 리뷰
  - [ ] HPA 동작 검증 (5개 앱 중 트래픽 변동 큰 2개)
  - [ ] P0/P1 이슈 목록 0건 (또는 fix 완료)
  - [ ] 핸드오프 문서 마지막 검토
```

New:
```markdown
- **Done When**:
  - [ ] AWS Cost Explorer 태그 기반 비용 가시성 확보
  - [ ] Resource request/limit 적정성 1회 리뷰
  - [ ] HPA 동작 검증 (5개 앱 중 트래픽 변동 큰 2개)
  - [ ] P0/P1 이슈 목록 0건 (또는 fix 완료)
  - [ ] 핸드오프 문서 마지막 검토
  - [ ] kustomize build 결과 캐싱 (CI 속도 개선, 선택) — W1 이월 (D-041)
```

- [ ] **Step 7: 검증**

Run:
```powershell
Write-Host "--- W1 PR diff/kustomize 캐싱 잔존 미체크 확인 (0이어야 함) ---"
Select-String -Path "docs/project-management/workflow/WORKFLOW_gitops_W1.md","docs/project-management/task/TASK_gitops.md" -Pattern '^\s*- \[ \].*(PR.*diff|kustomize build 결과 캐싱)' | Measure-Object | Select-Object Count

Write-Host "--- W5에 이월 박스 추가 확인 ---"
Select-String -Path "docs/project-management/workflow/WORKFLOW_gitops_W5.md" -Pattern 'W1 Step.*이월|W1 이월' | Measure-Object | Select-Object Count
```
Expected: 첫 카운트 = 0. 둘째 = 3 (Step 11: 2 + Step 12: 1).

- [ ] **Step 8: Commit**

```powershell
git add docs/project-management/workflow/WORKFLOW_gitops_W1.md docs/project-management/workflow/WORKFLOW_gitops_W5.md docs/project-management/task/TASK_gitops.md
git commit -m @'
docs(pm): W1 Step 3 3항목 → W5 Step 11/12 이월 (Phase C.2)

- WORKFLOW W1 3.1: PR diff 코멘트 도구 후보 → W5 Step 11
- WORKFLOW W1 3.2: PR diff 액션 → W5 Step 11
- WORKFLOW W1 3.2: kustomize 캐싱 → W5 Step 12
- TASK W1 Step 3 Done When 1건 → W5 Step 11/12 Done When 2건

사유: 선택 항목, W3 이월 표시 후 W3에서 미진행. Runbook+안정화 단계로 이동.

Refs: D-041
Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
'@
```

---

## Task 9: [Phase C.3] W2 Step 4 3항목 → W4 Step 9 이월

**Files:**
- Modify: `docs/project-management/workflow/WORKFLOW_gitops_W2.md` (라인 15, 29, 37 부근)
- Modify: `docs/project-management/workflow/WORKFLOW_gitops_W4.md` (Step 9 1.3에 추가)

- [ ] **Step 1: WORKFLOW_W2 Step 4.1 사전 분석 — dev 도메인 패턴 제거 + 주석화**

Old (Step 4 1.1):
```markdown
- [x] 5개 앱별 리소스 요구사항 수집 (메모리/CPU/replica)
- [x] 환경 변수 / ConfigMap 목록 정리
- [x] Service 포트 / Health endpoint 표준 합의
- [ ] dev 전용 도메인 패턴 결정 (dev-<app>.<도메인>) — EKS 배포 후
```

New:
```markdown
- [x] 5개 앱별 리소스 요구사항 수집 (메모리/CPU/replica)
- [x] 환경 변수 / ConfigMap 목록 정리
- [x] Service 포트 / Health endpoint 표준 합의
<!-- 2026-05-28 D-041로 W4 Step 9 (prod 도메인 흐름)로 이월: dev 전용 도메인 패턴 결정 (dev-<app>.<도메인>). 사유: 도메인 미확보, staging-<app> 패턴과 묶어 prod 도메인 확보 시 동시 결정. -->
```

- [ ] **Step 2: WORKFLOW_W2 Step 4.3 dev overlay 작성 — Ingress/LB 제거 + 주석화**

Old (Step 4 1.3):
```markdown
- [x] dev용 replicaCount=1, resources.requests 최소화
- [x] dev 전용 ConfigMap patch (LOG_LEVEL=DEBUG 등)
- [ ] Ingress 또는 Service LoadBalancer 정의 — EKS 배포 후
```

New:
```markdown
- [x] dev용 replicaCount=1, resources.requests 최소화
- [x] dev 전용 ConfigMap patch (LOG_LEVEL=DEBUG 등)
<!-- 2026-05-28 D-041로 W4 Step 9로 이월: Ingress 또는 Service LoadBalancer 정의. 사유: ACM/도메인 확보 후. -->
```

- [ ] **Step 3: WORKFLOW_W2 Step 4.4 적용+검증 — dev 도메인 5앱 도달 제거 + 주석화**

Task 2에서 이미 다른 3건은 ✅ 처리됨. 이제 마지막 1건 처리.

Old (Step 4 1.4 마지막 부분):
```markdown
- [x] /actuator/health 200 응답 (5/5 — 9차 세션 5/5 Healthy 달성)
- [ ] dev 도메인으로 5개 앱 도달 — Ingress 미설정
- [x] KAFKA_BROKERS endpoint 갱신 (PR #34, terraform re-apply 후 MSK 주소 변경)
```

New:
```markdown
- [x] /actuator/health 200 응답 (5/5 — 9차 세션 5/5 Healthy 달성)
<!-- 2026-05-28 D-041로 W4 Step 9로 이월: dev 도메인으로 5개 앱 도달. 사유: Ingress 미설정, ACM/도메인 확보 후. -->
- [x] KAFKA_BROKERS endpoint 갱신 (PR #34, terraform re-apply 후 MSK 주소 변경)
```

- [ ] **Step 4: WORKFLOW_W4 Step 9 1.3 적용+검증에 3항목 추가**

Old (Step 9 1.3):
```markdown
### 1.3 적용 + 검증
- [ ] prod 매니페스트 적용 (단, Application은 syncPolicy 없음)
- [ ] dev/staging → prod 승격 PR 1회 시뮬레이션
- [ ] 권한 없는 계정으로 prod sync 시도 → 거부 확인
- [ ] 권한 있는 계정으로 prod sync → 성공 확인
- [ ] prod 도메인으로 5개 앱 응답 확인
```

New:
```markdown
### 1.3 적용 + 검증
- [ ] prod 매니페스트 적용 (단, Application은 syncPolicy 없음)
- [ ] dev/staging → prod 승격 PR 1회 시뮬레이션
- [ ] 권한 없는 계정으로 prod sync 시도 → 거부 확인
- [ ] 권한 있는 계정으로 prod sync → 성공 확인
- [ ] prod 도메인으로 5개 앱 응답 확인
- [ ] dev 전용 도메인 패턴 적용 (dev-<app>.<도메인>) — W2 Step 4.1에서 이월 (D-041)
- [ ] dev Ingress 또는 Service LoadBalancer 정의 + 적용 — W2 Step 4.3에서 이월 (D-041)
- [ ] dev 도메인으로 5개 앱 도달 (HTTP 200) — W2 Step 4.4에서 이월 (D-041)
```

- [ ] **Step 5: 검증**

Run:
```powershell
Write-Host "--- W2 dev 도메인/Ingress 잔존 미체크 확인 (0이어야 함) ---"
Select-String -Path "docs/project-management/workflow/WORKFLOW_gitops_W2.md" -Pattern '^\s*- \[ \].*(dev 전용 도메인|Ingress 또는 Service|dev 도메인으로 5개)' | Measure-Object | Select-Object Count

Write-Host "--- W4 Step 9 1.3에 이월 박스 3건 신규 ---"
Select-String -Path "docs/project-management/workflow/WORKFLOW_gitops_W4.md" -Pattern 'W2 Step 4.*이월' | Measure-Object | Select-Object Count
```
Expected: 첫 카운트 = 0. 둘째 = 3.

- [ ] **Step 6: Commit**

```powershell
git add docs/project-management/workflow/WORKFLOW_gitops_W2.md docs/project-management/workflow/WORKFLOW_gitops_W4.md
git commit -m @'
docs(pm): W2 Step 4 3항목 → W4 Step 9 이월 (Phase C.3)

- WORKFLOW W2 4.1: dev 도메인 패턴 → W4 Step 9 1.3
- WORKFLOW W2 4.3: Ingress/LB 정의 → W4 Step 9 1.3
- WORKFLOW W2 4.4: dev 도메인 5앱 도달 → W4 Step 9 1.3

사유: 도메인 미확보, ACM/Route53 zone과 함께 prod 도메인 흐름에서 처리.

Refs: D-041
Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
'@
```

---

## Task 10: [Phase C.4] W2 Step 5 ESO 알람 → W3 Step 8 이월

**Files:**
- Modify: `docs/project-management/workflow/WORKFLOW_gitops_W2.md` (라인 70)
- Modify: `docs/project-management/workflow/WORKFLOW_gitops_W3.md` (Step 8 1.4)

- [ ] **Step 1: WORKFLOW_W2 Step 5.4 — ESO sync 알람 제거 + 주석화**

Old (Step 5 1.4):
```markdown
- [x] gitleaks 또는 trufflehog로 git history 스캔 → 0건 (gitleaks 8.30.1 / 114 commits / 0 leaks, 2026-05-26)
- [ ] ESO sync 실패 시 알람 설정 — W3 Observability
- [x] README에 새 시크릿 추가 절차 문서화
```

New:
```markdown
- [x] gitleaks 또는 trufflehog로 git history 스캔 → 0건 (gitleaks 8.30.1 / 114 commits / 0 leaks, 2026-05-26)
<!-- 2026-05-28 D-041로 W3 Step 8 (Observability)로 이월: ESO sync 실패 시 알람 설정. 사유: W3 PrometheusRule + Alertmanager 스택에 알람 룰 추가가 자연스러움. -->
- [x] README에 새 시크릿 추가 절차 문서화
```

- [ ] **Step 2: WORKFLOW_W3 Step 8.4 — ESO 알람 룰 추가**

Old (Step 8 1.4):
```markdown
### 1.4 알람 + 문서화
- [x] PrometheusRule: Pod 다운 5분 → critical
- [x] PrometheusRule: 메모리 90% 10분 → warning
- [x] PrometheusRule: 5xx 5% 5분 → critical
- [x] Alertmanager → Slack 라우팅 (실 webhook, A2 라우팅 확인)
- [x] 알람 발화 → slack receiver 라우팅 확인 (채널 수신은 눈 확인)
- [x] 대시보드/알람 README (`infra/monitoring/README.md`)
```

New:
```markdown
### 1.4 알람 + 문서화
- [x] PrometheusRule: Pod 다운 5분 → critical
- [x] PrometheusRule: 메모리 90% 10분 → warning
- [x] PrometheusRule: 5xx 5% 5분 → critical
- [x] Alertmanager → Slack 라우팅 (실 webhook, A2 라우팅 확인)
- [x] 알람 발화 → slack receiver 라우팅 확인 (채널 수신은 눈 확인)
- [x] 대시보드/알람 README (`infra/monitoring/README.md`)
- [ ] ESO sync 실패 PrometheusRule 추가 (`external_secrets_sync_calls_error{}` rate) — W2 Step 5.4에서 이월 (D-041)
```

- [ ] **Step 3: 검증**

Run:
```powershell
Write-Host "--- W2 ESO 알람 잔존 미체크 확인 (0이어야 함) ---"
Select-String -Path "docs/project-management/workflow/WORKFLOW_gitops_W2.md" -Pattern '^\s*- \[ \].*ESO sync 실패 시 알람' | Measure-Object | Select-Object Count

Write-Host "--- W3 Step 8에 이월 박스 1건 신규 ---"
Select-String -Path "docs/project-management/workflow/WORKFLOW_gitops_W3.md" -Pattern 'W2 Step 5.*이월' | Measure-Object | Select-Object Count
```
Expected: 첫 카운트 = 0. 둘째 = 1.

- [ ] **Step 4: Commit**

```powershell
git add docs/project-management/workflow/WORKFLOW_gitops_W2.md docs/project-management/workflow/WORKFLOW_gitops_W3.md
git commit -m @'
docs(pm): W2 Step 5 ESO 알람 → W3 Step 8 이월 (Phase C.4)

- WORKFLOW W2 5.4: ESO sync 실패 알람 → 주석화
- WORKFLOW W3 8.4: ESO sync 실패 PrometheusRule 추가 박스 신설

사유: W3 PrometheusRule + Alertmanager 스택에 자연스럽게 추가. W3는 기존 항목 모두 Done이라 진척률 영향 미미.

Refs: D-041
Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
'@
```

---

## Task 11: [Phase C.5] W2 Step 6 3항목 → W4 Step 10 이월

**Files:**
- Modify: `docs/project-management/workflow/WORKFLOW_gitops_W2.md` (라인 93, 94, 95)
- Modify: `docs/project-management/workflow/WORKFLOW_gitops_W4.md` (Step 10 1.2)

- [ ] **Step 1: WORKFLOW_W2 Step 6.3 — 3항목 제거 + 주석화**

Old (Step 6 1.3):
```markdown
### 1.3 적용 + 검증
- [ ] 5개 앱 모두 새 이미지 푸시 → dev 자동 반영 확인 — EKS 배포 후
- [ ] 평균 반영 시간 측정 (목표 5분 이내) — EKS 배포 후
- [ ] 롤백 케이스: 잘못된 이미지 → 이전 태그로 복귀 가능 — EKS 배포 후
- [x] image 태그 변경 이력이 git log에 남는지 확인 (write-back-method: git 설정)
```

New:
```markdown
### 1.3 적용 + 검증
<!-- 2026-05-28 D-041로 W4 Step 10 (롤백/백업)로 이월 3건:
     - 5개 앱 모두 새 이미지 푸시 → dev 자동 반영 확인
     - 평균 반영 시간 측정 (목표 5분 이내)
     - 롤백 케이스: 잘못된 이미지 → 이전 태그로 복귀 가능
     사유: main 보호 ruleset 직접 push 거부 + overlay semver 전략 불일치. A안(전용 봇 bypass) 실행은 W4+에서 진행 — image-updater-ecr-setup.md "A 실행 절차" 참조. -->
- [x] image 태그 변경 이력이 git log에 남는지 확인 (write-back-method: git 설정)
```

- [ ] **Step 2: WORKFLOW_W4 Step 10 1.2 롤백 절차 구현에 3항목 추가**

Old (Step 10 1.2):
```markdown
### 1.2 롤백 절차 구현
- [ ] ArgoCD History rollback 절차 검증 (UI에서 1 step 이전 sync)
- [ ] git revert + 자동 sync로 롤백 절차 검증
- [ ] Image Updater 사용 시 태그 강제 고정 절차
- [ ] DB 마이그레이션이 포함된 경우 롤백 가이드 (forward-only 정책 등)
```

New:
```markdown
### 1.2 롤백 절차 구현
- [ ] ArgoCD History rollback 절차 검증 (UI에서 1 step 이전 sync)
- [ ] git revert + 자동 sync로 롤백 절차 검증
- [ ] Image Updater 사용 시 태그 강제 고정 절차
- [ ] DB 마이그레이션이 포함된 경우 롤백 가이드 (forward-only 정책 등)
- [ ] Image Updater 5개 앱 새 이미지 푸시 → dev 자동 반영 E2E 검증 — W2 Step 6.3에서 이월 (D-041, A안 실행 필요)
- [ ] Image Updater 평균 반영 시간 측정 (목표 5분 이내) — W2 Step 6.3에서 이월 (D-041)
- [ ] Image Updater 롤백 케이스: 잘못된 이미지 → 이전 태그 복귀 가능 — W2 Step 6.3에서 이월 (D-041)
```

- [ ] **Step 3: 검증**

Run:
```powershell
Write-Host "--- W2 Step 6.3 자동 반영/반영 시간/롤백 케이스 잔존 미체크 (0이어야 함) ---"
Select-String -Path "docs/project-management/workflow/WORKFLOW_gitops_W2.md" -Pattern '^\s*- \[ \].*(5개 앱 모두 새 이미지|평균 반영 시간|롤백 케이스: 잘못된 이미지)' | Measure-Object | Select-Object Count

Write-Host "--- W4 Step 10에 이월 박스 3건 신규 ---"
Select-String -Path "docs/project-management/workflow/WORKFLOW_gitops_W4.md" -Pattern 'W2 Step 6.*이월' | Measure-Object | Select-Object Count
```
Expected: 첫 카운트 = 0. 둘째 = 3.

- [ ] **Step 4: Commit**

```powershell
git add docs/project-management/workflow/WORKFLOW_gitops_W2.md docs/project-management/workflow/WORKFLOW_gitops_W4.md
git commit -m @'
docs(pm): W2 Step 6 3항목 → W4 Step 10 이월 (Phase C.5)

- WORKFLOW W2 6.3: 자동 반영 E2E / 평균 반영 시간 / 롤백 케이스 → 주석화
- WORKFLOW W4 10.2: 위 3항목을 롤백 절차 구현 박스로 신설

사유: main 보호 ruleset + semver 전략 블록. A안(전용 봇 bypass) 실행 절차는 image-updater-ecr-setup.md 참조.

Refs: D-041, FR-GO-205
Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
'@
```

---

## Task 12: [Phase D] TASK_gitops.md Status 라인 6개 갱신

**Files:**
- Modify: `docs/project-management/task/TASK_gitops.md` (Step 1/2/3 Status 3라인 + Step 4/5/6 Status 3라인)

- [ ] **Step 1: W1 Step 1 Status 갱신**

Old:
```markdown
**Status**: [ ] Not Started / [ ] In Progress / [x] Done (옵션2 적용 + kind B-2 실증 완료, FR-GO-102 일부 W2 이월. 실 EKS B-1은 결제수단 verification 후)
```

New:
```markdown
**Status**: [ ] Not Started / [ ] In Progress / [x] Done (옵션2 적용 + kind B-2 실증. FR-GO-102 4항목[ACM/DNS/외부TLS/webhook] W4 Step 9로 이월 — D-041)
```

- [ ] **Step 2: W1 Step 2 Status 갱신**

Old:
```markdown
**Status**: [ ] Not Started / [ ] In Progress / [x] Done
```

New:
```markdown
**Status**: [ ] Not Started / [ ] In Progress / [x] Done (root.yaml은 D-002로 채택 안 함, 선택 항목 정리 — D-041)
```

- [ ] **Step 3: W1 Step 3 Status 갱신**

Old:
```markdown
**Status**: [ ] Not Started / [ ] In Progress / [x] Done (PR diff 코멘트는 W3 이월)
```

New:
```markdown
**Status**: [ ] Not Started / [ ] In Progress / [x] Done (PR diff 코멘트·kustomize 캐싱 3항목 W5 Step 11/12로 이월 — D-041)
```

- [ ] **Step 4: W2 Step 4 Status 갱신**

Old:
```markdown
**Status**: [ ] Not Started / [x] In Progress / [ ] Done (EKS 3/5 Healthy, 2개 앱 레벨 문제 잔존)
```

New:
```markdown
**Status**: [ ] Not Started / [ ] In Progress / [x] Done (EKS 5/5 Healthy — PR #38 + PR #40. dev 도메인 패턴/Ingress/도메인 도달 3항목 W4 Step 9로 이월 — D-041)
```

- [ ] **Step 5: W2 Step 5 Status 갱신**

Old:
```markdown
**Status**: [ ] Not Started / [ ] In Progress / [x] Done (EKS 실배포 완료: ESO Helm + IRSA + ClusterSecretStore Valid + 5개 SecretSynced)
```

New:
```markdown
**Status**: [ ] Not Started / [ ] In Progress / [x] Done (EKS 실배포 완료: ESO Helm + IRSA + ClusterSecretStore Valid + 5개 SecretSynced. ESO sync 실패 알람 1항목 W3 Step 8로 이월 — D-041)
```

- [ ] **Step 6: W2 Step 6 Status 갱신**

Old:
```markdown
**Status**: [ ] Not Started / [ ] In Progress / [x] Done (매니페스트 + ECR 교체 완료, E2E 검증은 EKS 배포 후)
```

New:
```markdown
**Status**: [ ] Not Started / [ ] In Progress / [x] Done (매니페스트 + ECR 교체 + 자동 sync 비활성화·svc팀 공유 문서화 완료. 이미지 E2E 3항목 W4 Step 10으로 이월 — D-041)
```

- [ ] **Step 7: 검증 — W1/W2 Status 라인 모두 Done**

Run:
```powershell
Write-Host "--- W1/W2 모든 Status 라인이 [x] Done이고 In Progress가 0건 ---"
Select-String -Path "docs/project-management/task/TASK_gitops.md" -Pattern '^\*\*Status\*\*.*\[x\] In Progress' | Measure-Object | Select-Object Count
```
Expected: 0.

Run:
```powershell
Select-String -Path "docs/project-management/task/TASK_gitops.md" -Pattern '^\*\*Status\*\*' | Select-Object LineNumber,Line
```
Expected: W1 Step 1/2/3 + W2 Step 4/5/6 + W3 Step 7/8 + W4 Step 9/10 + W5 Step 11/12 — 12라인. W1/W2 6개는 모두 `[x] Done`. W3 2개도 `[x] Done`. W4/W5 4개는 `[ ] Done` (Not Started).

- [ ] **Step 8: Commit**

```powershell
git add docs/project-management/task/TASK_gitops.md
git commit -m @'
docs(pm): TASK Status 라인 6개 갱신 (Phase D)

- W1 Step 1: 4항목 W4 이월 메모
- W1 Step 2: D-002 결정 메모
- W1 Step 3: 3항목 W5 이월 메모
- W2 Step 4: In Progress(3/5) → Done(5/5) + 3항목 W4 이월
- W2 Step 5: + 1항목 W3 이월
- W2 Step 6: + 3항목 W4 이월

W1/W2 모든 Step Status = [x] Done으로 통일.

Refs: D-041
Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
'@
```

---

## Task 13: [Phase E.1] HISTORY_gitops.md에 D-041 추가

**Files:**
- Modify: `docs/project-management/history/HISTORY_gitops.md` (파일 끝 또는 적절한 날짜 섹션에 추가)

- [ ] **Step 1: 파일 끝에 새 섹션 append**

기존 HISTORY 파일 끝에 다음 블록을 추가:

```markdown

---

## 2026-05-28 (W4 Day -3) — W1/W2 PM 문서 100% 정합화

### 의사결정
- **D-041 W1/W2 PM 문서 100% 정합화 + 이월 정책 (2026-05-28)**:
  - **결정**: W1/W2의 미해결 박스 14건을 다음 주차(W3/W4/W5)로 **물리적 라인 이동**. 원자리에는 HTML 주석으로 사유 + 결정 ID 흔적 유지. TASK 본문 미체크 박스 3건도 동일 정책. TASK_gitops.md W1/W2 Step Status는 모두 `[x] Done`으로 통일하고 각 라인에 "X항목 이월" 메모 첨부.
  - **이유**: W4 prod 진입 시점에 PM 대시보드가 W2=In Progress(3/5)로 표시되어 실제(5/5 Healthy)와 불일치. WORKFLOW 파서가 `- [ ]` / `- [x]` 박스를 카운트하므로 W1/W2 = 100%를 위해 박스 자체를 이동.
  - **대안 검토**:
    - 원자리 `[x]` + 사유: 100% 즉시 달성하나 W3+ 박스도 추가 시 카운트 이중.
    - 라인 제거 + 줄글 전환: 100% 달성하나 W1/W2에서 흔적 사라짐.
  - **결과**:
    - WORKFLOW W1 unchecked: 8 → 0 (1건 사실반영 + 7건 이월)
    - WORKFLOW W2 unchecked: 13 → 0 (4건 사실반영 + 2건 신규 + 7건 이월)
    - WORKFLOW W3 unchecked: 1 → 2 (+1 ESO 알람)
    - WORKFLOW W4 unchecked: +10 (Step 9: +7, Step 10: +3)
    - WORKFLOW W5 unchecked: +3 (Step 11: +2, Step 12: +1)
    - TASK W1/W2 Status 6 Step 모두 `[x] Done`으로 통일

### 산출물
- 디자인 스펙: `docs/superpowers/specs/2026-05-28-w1-w2-100pct-design.md`
- 구현 플랜: `docs/superpowers/plans/2026-05-28-w1-w2-100pct.md`
- Runbook 패치: `docs/runbooks/image-updater-ecr-setup.md` (자동 sync 비활성화 절차 섹션 추가)
- PM 문서 갱신: TASK_gitops.md, WORKFLOW_gitops_W1.md ~ W5.md

### 이벤트
- W2 Step 4 사실 갱신: 9차 세션(PR #40 + PR #38)에서 이미 5/5 Healthy 달성한 사실을 본 작업에서 PM 문서에 반영. TASK는 In Progress(3/5)로, WORKFLOW는 5/5 미반영 상태로 방치되어 있었음.
- 단일 PR로 묶음: `chore/w1-w2-100pct-pm-consolidation` 브랜치.
```

- [ ] **Step 2: 검증**

Run:
```powershell
Select-String -Path "docs/project-management/history/HISTORY_gitops.md" -Pattern "D-041|2026-05-28" | Select-Object LineNumber,Line
```
Expected: 새 섹션 헤더 + D-041 결정 라인 등이 보임.

- [ ] **Step 3: Commit**

```powershell
git add docs/project-management/history/HISTORY_gitops.md
git commit -m @'
docs(history): D-041 W1/W2 PM 문서 100% 정합화 결정 기록 (Phase E.1)

- 미해결 박스 14건 W3/W4/W5 물리적 이동 + 사유 주석 정책
- W2 Step 4 5/5 Healthy 사실 반영 (PR #38, #40)
- 박스 변화 카운트 매트릭스 명시

Refs: D-041, D-001, D-002
Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
'@
```

---

## Task 14: [Phase E.2] 최종 검증

**Files:**
- Verify: 모든 변경 파일

- [ ] **Step 1: WORKFLOW W1/W2 박스 카운트 = 0 검증**

Run:
```powershell
Write-Host "=== 최종 검증 ==="
foreach ($w in @('W1','W2','W3','W4','W5')) {
  $path = "docs/project-management/workflow/WORKFLOW_gitops_$w.md"
  $unchecked = (Get-Content $path | Select-String -Pattern '^- \[ \]' -SimpleMatch:$false).Count
  $checked   = (Get-Content $path | Select-String -Pattern '^- \[x\]' -SimpleMatch:$false).Count
  $total = $unchecked + $checked
  $pct = if ($total -gt 0) { [math]::Round(($checked / $total) * 100, 1) } else { 0 }
  Write-Host "$w : unchecked=$unchecked, checked=$checked, total=$total, pct=$pct%"
}
```
Expected:
- W1: unchecked=0, pct=100%
- W2: unchecked=0, pct=100%
- W3: unchecked=2, pct ≈ 88% (앱별 상세 대시보드 1개 + ESO 알람 1개)
- W4: unchecked baseline+10
- W5: unchecked baseline+3

- [ ] **Step 2: TASK Step Status 확인**

Run:
```powershell
Select-String -Path "docs/project-management/task/TASK_gitops.md" -Pattern '^\*\*Status\*\*' | Select-Object LineNumber,Line
Write-Host "--- W1/W2 In Progress 잔존 (0이어야 함) ---"
Select-String -Path "docs/project-management/task/TASK_gitops.md" -Pattern '^\*\*Status\*\*.*\[x\] In Progress' | Measure-Object | Select-Object Count
```
Expected: 6개(W1/W2 Step 1~6) 모두 `[x] Done`. In Progress = 0건.

- [ ] **Step 3: D-041 흔적 분포 확인**

Run:
```powershell
Get-ChildItem -Recurse -Path "docs/project-management","docs/runbooks","docs/superpowers" -Filter "*.md" | ForEach-Object {
  $count = (Select-String -Path $_.FullName -Pattern "D-041" -SimpleMatch:$false).Count
  if ($count -gt 0) { Write-Host "$($_.FullName) : $count 회 언급" }
}
```
Expected: spec, plan, HISTORY, TASK + WORKFLOW W1/W2/W3/W4/W5 + 일부 주석 파일들 — 다수 파일에서 D-041 언급 확인.

- [ ] **Step 4: 모든 변경 사항 git diff 요약**

Run:
```powershell
git log --oneline main..HEAD
git diff --stat main..HEAD
```
Expected: 약 12개 commit + 변경 파일 약 8개.

- [ ] **Step 5: 검증 결과를 PR description용 메모로 저장**

플랜 작성자가 작성, 별도 commit 없음. PR 본문에 위 검증 결과 붙여 넣을 준비.

---

## Task 15: [Phase E.3] push + PR 생성

**Files:**
- 새 PR 생성

- [ ] **Step 1: 원격 브랜치로 push**

Run:
```powershell
git push -u origin chore/w1-w2-100pct-pm-consolidation
```
Expected: 새 브랜치 생성 + GitHub URL 출력.

- [ ] **Step 2: PR 생성 (gh CLI)**

Run:
```powershell
gh pr create --base main --head chore/w1-w2-100pct-pm-consolidation --title "chore(pm): W1/W2 PM 문서 100% 정합화 + 이월 처리 (D-041)" --body @'
## 변경 요약

W4 prod 진입을 앞두고 PM 문서를 실제 진척에 정합화합니다.

- W2 Step 4가 실제로는 **5/5 Healthy**인데 TASK는 `In Progress (3/5)`로 표시되어 있던 불일치 해소 (HANDOFF_W2 §2 / PR #38 / PR #40 참조).
- 즉시 진행 불가한 미해결 박스 **14건**을 다음 주차(W3/W4/W5)로 **물리적 라인 이동** + 원자리에 사유 주석 유지.
- 즉시 처리 가능한 W2 Step 6 문서화 2건(자동 sync 비활성화 절차 + svc팀 배포 흐름 cross-ref) 처리.

## 카운트 변화

| 파일 | unchecked: 시작 → 종료 |
|---|---|
| WORKFLOW_W1 | 8 → **0** (100%) |
| WORKFLOW_W2 | 13 → **0** (100%) |
| WORKFLOW_W3 | 1 → 2 (+1 ESO 알람) |
| WORKFLOW_W4 | baseline → baseline + 10 (Step 9: +7, Step 10: +3) |
| WORKFLOW_W5 | baseline → baseline + 3 (Step 11: +2, Step 12: +1) |
| TASK W1/W2 Status | 6 Step 모두 `[x] Done` |

## 관련 문서

- 설계: `docs/superpowers/specs/2026-05-28-w1-w2-100pct-design.md`
- 플랜: `docs/superpowers/plans/2026-05-28-w1-w2-100pct.md`
- 결정 기록: HISTORY D-041

## 검증

```powershell
foreach ($w in @('W1','W2','W3','W4','W5')) {
  $path = "docs/project-management/workflow/WORKFLOW_gitops_$w.md"
  $unchecked = (Get-Content $path | Select-String -Pattern '^- \[ \]').Count
  Write-Host "$w unchecked = $unchecked"
}
```

기대: W1=0, W2=0.

## 영향 범위

- `workflow-dashboard` GitHub Pages 진척률 표시: W1=100%, W2=100% 달성.
- 운영 코드(매니페스트/스크립트) 변경 없음.
- W3 진척률 약간 하락(의도 — ESO 알람 박스 +1, 기존 다른 박스 모두 Done이므로 영향 미미).

Refs: D-041, D-001, D-002, FR-GO-205
'@
```
Expected: PR URL 출력.

- [ ] **Step 3: PR 생성 후 CI 통과 확인**

Run:
```powershell
gh pr checks (gh pr view --json number -q .number)
```
Expected: `Validate Kubernetes Manifests` 등 모든 CI 체크 PASS (PM 문서만 변경 — 매니페스트 변경 없으므로 패스 보장).

- [ ] **Step 4: 사용자에게 머지 권한 위임**

머지는 사용자가 직접 수행하거나 본 에이전트에게 명시 요청. 본 플랜 종료.

---

## Self-Review (플랜 작성자)

**1. Spec coverage**:
- Phase A 8건 → Task 2/3/4 (8건 처리)
- Phase B 2건 → Task 5/6 (2건 처리)
- Phase C 14건 → Task 7/8/9/10/11 (14건 처리)
- Phase D 6 Status → Task 12 (6건 처리)
- Phase E HISTORY + PR → Task 13/14/15 (3 phase 처리)

스펙 §4에서 "이월 13건"으로 표기했으나 실제 카운트는 14건(W1 4건 + W1 3건 + W2 3건 + W2 1건 + W2 3건). 플랜과 PR description에 14건으로 정정 표기.

**2. Placeholder scan**: TODO/TBD/"implement later" 없음. 모든 코드/주석 블록 실제 내용 포함.

**3. Type consistency**: 모든 D-041 참조 일관, 파일 경로 정확, Step 번호 정확.

**4. Phase B 자동 sync 비활성화 절차 코드 검증**: ApplicationSet의 `spec.template.spec.syncPolicy.automated` 구조는 표준 ApplicationSet 스펙. `argocd app set --auto-prune=false --self-heal=false`는 표준 CLI 옵션. `kubectl scale deploy argocd-image-updater --replicas=0`는 표준 kubectl 명령. 모두 검증된 패턴.

스펙과 일치, 누락 없음.
