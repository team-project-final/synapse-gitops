# W5 진입 차단 클리어 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** gitops 잔여 차단 5건(#91/#92/#120/#121/#122)을 on-demand EKS+MSK window 1회로 클리어하기 위한 Phase 0 산출물(코드·스크립트·런북)을 작성하고 PR로 머지한다.

**Architecture:** 4건(#91/#92/#120/#122)은 라이브 검증, #121만 신규 코드. #121 TLS는 ALB + self-signed ACM import(nip.io 임시 도메인)로 리포의 ALB 일관성을 유지한다. ArgoCD UI는 현재 `argocd-server` NLB 서비스로 노출되므로, nip.io ALB ingress는 이를 타깃하는 **추가(additive)** 구성이다. 라이브 검증 절차는 런북 문서로 구동한다.

**Tech Stack:** Kubernetes Ingress(AWS ALB / `aws-load-balancer-controller`), Bash + openssl + aws-cli(ACM import), Terraform(kafka-topics, Mongey/kafka provider), ArgoCD Image Updater.

**참고 (이 plan의 경계):**
- `verify-argocd-deploy.sh`는 **synapse-shared 소속이며 team-lead가 bastion SSM에서 실행**(이슈 #91). 본 plan은 호출을 런북에 기술할 뿐 스크립트를 작성하지 않는다.
- 라이브 검증(Phase 1~4)은 on-demand window에서 수행 — 본 plan은 그 절차를 담은 **런북**과, window 전에 main에 머지할 **Phase 0 산출물**까지를 구현 대상으로 한다.

---

## File Structure

| 파일 | 책임 | 작업 |
|------|------|------|
| `infra/aws/dev/kafka-topics/.terraform.lock.hcl` | provider 잠금(재현성) | Commit (현재 untracked) |
| `infra/aws/dev/k8s-kafka-config/.terraform.lock.hcl` | provider 잠금(재현성) | Commit (현재 untracked) |
| `infra/ingress/nipio/argocd-server-ingress.yaml` | ArgoCD UI+webhook nip.io ALB 노출(self-signed ACM) | Create |
| `infra/ingress/nipio/dev-ingress.yaml` | dev gateway nip.io ALB 노출 | Create |
| `infra/ingress/nipio/README.md` | nip.io 임시 노출 의도·치환·폐기 전제 문서화 | Create |
| `scripts/gen-nipio-selfsigned.sh` | ALB IP → self-signed CA+leaf 생성 → ACM import → ARN 출력 | Create |
| `docs/runbooks/W5_CLEARANCE_WINDOW.md` | Phase 0~4 라이브 실행 체크리스트 | Create |

기존 `infra/ingress/dev-ingress.yaml`·`staging-ingress.yaml`(실 도메인+ACM용)은 **보존**(prod 사이클 자산). nip.io 변형은 `infra/ingress/nipio/` 하위에 분리해 혼동을 막는다.

---

## Task 1: terraform lock 파일 커밋

**Files:**
- Commit: `infra/aws/dev/kafka-topics/.terraform.lock.hcl`
- Commit: `infra/aws/dev/k8s-kafka-config/.terraform.lock.hcl`

- [ ] **Step 1: lock 파일이 Mongey/kafka provider를 고정하는지 확인**

Run: `grep -E "provider|version" infra/aws/dev/kafka-topics/.terraform.lock.hcl`
Expected: `provider "registry.terraform.io/mongey/kafka"` 와 `version` 라인이 보인다.

- [ ] **Step 2: 두 lock 파일 커밋**

```bash
git add infra/aws/dev/kafka-topics/.terraform.lock.hcl infra/aws/dev/k8s-kafka-config/.terraform.lock.hcl
git commit -m "chore(infra): kafka-topics/k8s-kafka-config provider lock 커밋 (#120 재현성)"
```

---

## Task 2: nip.io argocd-server ingress 작성

**Files:**
- Create: `infra/ingress/nipio/argocd-server-ingress.yaml`

- [ ] **Step 1: 매니페스트 작성**

`<ALB_IP>`·`<ACM_ARN>`는 라이브에서 `gen-nipio-selfsigned.sh` 출력으로 치환하는 플레이스홀더다. `group.name`으로 dev ingress와 ALB 1개를 공유 → IP 1개 → nip.io 베이스 일관.

```yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: argocd-server-nipio
  namespace: argocd
  annotations:
    kubernetes.io/ingress.class: alb
    alb.ingress.kubernetes.io/scheme: internet-facing
    alb.ingress.kubernetes.io/target-type: ip
    alb.ingress.kubernetes.io/group.name: synapse-nipio
    alb.ingress.kubernetes.io/listen-ports: '[{"HTTPS":443}]'
    # argocd-server는 443에서 자체 TLS 종료 → ALB가 HTTPS로 백엔드 통신
    alb.ingress.kubernetes.io/backend-protocol: HTTPS
    alb.ingress.kubernetes.io/healthcheck-path: /healthz
    # 라이브: gen-nipio-selfsigned.sh 출력 CERT_ARN으로 치환
    alb.ingress.kubernetes.io/certificate-arn: <ACM_ARN>
spec:
  rules:
    # 라이브: <ALB_IP>를 ALB IP로 치환 (gen-nipio-selfsigned.sh가 동일 IP로 SAN 발급)
    - host: argocd.<ALB_IP>.nip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: argocd-server
                port:
                  number: 443
```

- [ ] **Step 2: yamllint 통과 확인**

Run: `yamllint infra/ingress/nipio/argocd-server-ingress.yaml`
Expected: 에러 없음 (160자 초과 warning 없음).

- [ ] **Step 3: 커밋**

```bash
git add infra/ingress/nipio/argocd-server-ingress.yaml
git commit -m "feat(ingress): #121 ArgoCD UI+webhook nip.io ALB ingress (self-signed ACM 전제)"
```

---

## Task 3: nip.io dev ingress 작성

**Files:**
- Create: `infra/ingress/nipio/dev-ingress.yaml`

- [ ] **Step 1: 매니페스트 작성**

dev는 기존 단일 진입 gateway 패턴 유지. dev-\<app\> 세분 도달은 gateway 경로 라우팅으로 충족.

```yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: synapse-dev-nipio
  namespace: synapse-dev
  annotations:
    kubernetes.io/ingress.class: alb
    alb.ingress.kubernetes.io/scheme: internet-facing
    alb.ingress.kubernetes.io/target-type: ip
    alb.ingress.kubernetes.io/group.name: synapse-nipio
    alb.ingress.kubernetes.io/listen-ports: '[{"HTTPS":443}]'
    alb.ingress.kubernetes.io/healthcheck-path: /actuator/health/readiness
    # 라이브: gen-nipio-selfsigned.sh 출력 CERT_ARN으로 치환
    alb.ingress.kubernetes.io/certificate-arn: <ACM_ARN>
spec:
  rules:
    # 라이브: <ALB_IP>를 ALB IP로 치환
    - host: dev.<ALB_IP>.nip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: gateway
                port:
                  number: 80
```

- [ ] **Step 2: yamllint 통과 확인**

Run: `yamllint infra/ingress/nipio/dev-ingress.yaml`
Expected: 에러 없음.

- [ ] **Step 3: 커밋**

```bash
git add infra/ingress/nipio/dev-ingress.yaml
git commit -m "feat(ingress): #121 dev gateway nip.io ALB ingress (group 공유)"
```

---

## Task 4: gen-nipio-selfsigned.sh 작성

**Files:**
- Create: `scripts/gen-nipio-selfsigned.sh`

- [ ] **Step 1: 스크립트 작성**

```bash
#!/usr/bin/env bash
# nip.io 임시 도메인용 self-signed 인증서 생성 + ACM import (#121, W5 clearance window)
# 전제: ALB 프로비저닝 완료(ingress apply 후), openssl/aws/dig 사용 가능.
set -euo pipefail

AWS_REGION="${AWS_REGION:-ap-northeast-2}"
OUTDIR="${OUTDIR:-./.nipio-certs}"
SKIP_IMPORT=false

log() { echo -e "\033[1;34m[*]\033[0m $*"; }
ok()  { echo -e "\033[1;32m[OK]\033[0m $*"; }
err() { echo -e "\033[1;31m[ERR]\033[0m $*" >&2; }

usage() {
  cat <<USAGE
사용법: gen-nipio-selfsigned.sh <ALB_DNS_또는_IP> [옵션]
  --skip-import   ACM import 생략(로컬 인증서 생성만 — 오프라인 검증용)
  --region <r>    AWS 리전 (기본 ap-northeast-2 / 환경변수 AWS_REGION)
  --out <dir>     출력 디렉토리 (기본 ./.nipio-certs)
  --help          도움말
출력:
  <out>/ca.crt, <out>/ca.key, <out>/leaf.crt, <out>/leaf.key
  ACM import 시 stdout 마지막 줄에 CERT_ARN=<arn>
USAGE
}

[ $# -ge 1 ] || { usage; exit 1; }
TARGET="$1"; shift
while [ $# -gt 0 ]; do
  case "$1" in
    --skip-import) SKIP_IMPORT=true ;;
    --region) AWS_REGION="$2"; shift ;;
    --out) OUTDIR="$2"; shift ;;
    --help) usage; exit 0 ;;
    *) err "알 수 없는 옵션: $1"; usage; exit 1 ;;
  esac
  shift
done

require() { command -v "$1" >/dev/null 2>&1 || { err "필수 도구 없음: $1"; exit 1; }; }
require openssl

# 입력이 IP면 그대로, hostname이면 dig로 IP 추출
if echo "$TARGET" | grep -qE '^[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+$'; then
  IP="$TARGET"
else
  require dig
  IP="$(dig +short "$TARGET" | grep -E '^[0-9]+\.' | head -n1)"
  [ -n "$IP" ] || { err "ALB DNS에서 IP 추출 실패: $TARGET"; exit 1; }
fi
log "대상 IP: $IP (nip.io 베이스: ${IP}.nip.io)"

mkdir -p "$OUTDIR"
ARGOCD_HOST="argocd.${IP}.nip.io"
DEV_HOST="dev.${IP}.nip.io"

# 1) self-signed CA (window 폐기 전제 → 7일 유효)
openssl req -x509 -newkey rsa:2048 -nodes -days 7 \
  -keyout "$OUTDIR/ca.key" -out "$OUTDIR/ca.crt" \
  -subj "/CN=synapse-nipio-ca"

# 2) leaf key + CSR
openssl req -newkey rsa:2048 -nodes \
  -keyout "$OUTDIR/leaf.key" -out "$OUTDIR/leaf.csr" \
  -subj "/CN=${ARGOCD_HOST}"

# 3) SAN 확장 (argocd/dev/와일드카드)
cat > "$OUTDIR/san.ext" <<EXT
subjectAltName = DNS:${ARGOCD_HOST}, DNS:${DEV_HOST}, DNS:*.${IP}.nip.io
EXT

# 4) CA로 leaf 서명
openssl x509 -req -in "$OUTDIR/leaf.csr" -days 7 \
  -CA "$OUTDIR/ca.crt" -CAkey "$OUTDIR/ca.key" -CAcreateserial \
  -extfile "$OUTDIR/san.ext" -out "$OUTDIR/leaf.crt"
ok "인증서 생성: $OUTDIR/leaf.crt (SAN: ${ARGOCD_HOST}, ${DEV_HOST}, *.${IP}.nip.io)"

if $SKIP_IMPORT; then
  ok "--skip-import: ACM import 생략(로컬 생성만)"
  exit 0
fi

# 5) ACM import → ARN
require aws
ARN="$(aws acm import-certificate \
  --region "$AWS_REGION" \
  --certificate "fileb://$OUTDIR/leaf.crt" \
  --private-key "fileb://$OUTDIR/leaf.key" \
  --certificate-chain "fileb://$OUTDIR/ca.crt" \
  --query CertificateArn --output text)"
ok "ACM import 완료 (region=$AWS_REGION)"
echo "CERT_ARN=${ARN}"
```

- [ ] **Step 2: 실행 권한 부여 + 구문 검사**

```bash
chmod +x scripts/gen-nipio-selfsigned.sh
bash -n scripts/gen-nipio-selfsigned.sh
```
Expected: 출력 없음(구문 OK).

- [ ] **Step 3: shellcheck (가능 시)**

Run: `shellcheck scripts/gen-nipio-selfsigned.sh || echo "shellcheck 미설치 — bash -n으로 대체"`
Expected: 경고 없음 또는 미설치 메시지.

- [ ] **Step 4: 오프라인 로컬 검증 (--skip-import)**

Run: `bash scripts/gen-nipio-selfsigned.sh 1.2.3.4 --skip-import --out /tmp/nipio-test`
Expected: `인증서 생성: /tmp/nipio-test/leaf.crt` 출력, exit 0.

- [ ] **Step 5: 생성된 인증서의 SAN 확인**

Run: `openssl x509 -in /tmp/nipio-test/leaf.crt -noout -text | grep -A1 "Subject Alternative Name"`
Expected: `DNS:argocd.1.2.3.4.nip.io, DNS:dev.1.2.3.4.nip.io, DNS:*.1.2.3.4.nip.io`

- [ ] **Step 6: 임시 산출물 정리 + 커밋**

```bash
rm -rf /tmp/nipio-test
git add scripts/gen-nipio-selfsigned.sh
git commit -m "feat(scripts): #121 nip.io self-signed 인증서 생성 + ACM import (--skip-import 로컬검증 지원)"
```

---

## Task 5: nip.io ingress README 작성

**Files:**
- Create: `infra/ingress/nipio/README.md`

- [ ] **Step 1: README 작성**

```markdown
# infra/ingress/nipio

**임시(폐기 전제) nip.io 외부 노출** — 실 도메인 부재 시 #121 검증용. ALB + self-signed ACM import.

## 파일
- `argocd-server-ingress.yaml` — ArgoCD UI + webhook(`/api/webhook`) → `argocd-server`(NLB 서비스에 추가되는 ALB ingress, backend-protocol HTTPS).
- `dev-ingress.yaml` — dev gateway 단일 진입 → `gateway:80`. dev-<app> 세분은 gateway 경로 라우팅으로 충족.

두 ingress는 `group.name: synapse-nipio`로 **ALB 1개를 공유** → IP 1개 → nip.io 베이스 일관.

## 라이브 치환 (W5 clearance window, Phase 3)
1. ingress apply (cert-arn 미설정 상태로 ALB 프로비저닝 트리거).
2. `kubectl get ingress -A` 에서 ALB DNS 확보.
3. `scripts/gen-nipio-selfsigned.sh <ALB_DNS>` → `CERT_ARN=...` 획득.
4. 두 ingress의 `<ALB_IP>`·`<ACM_ARN>` 치환 후 재apply.
5. `curl --cacert .nipio-certs/ca.crt https://argocd.<IP>.nip.io` → 200.

## 주의
- nip.io의 `<IP>`는 window 동안만 유효(ALB IP는 ephemeral). 폐기 전제 검증이라 의도된 동작.
- self-signed → "인증서 유효"는 CA 신뢰 주입(`--cacert`) 기반 체인 검증으로 충족.
- 실 도메인 확보 시 상위 `infra/ingress/dev-ingress.yaml`·`staging-ingress.yaml`(ACM 공인) 사용 — 본 디렉토리는 그때 폐기.
```

- [ ] **Step 2: 커밋**

```bash
git add infra/ingress/nipio/README.md
git commit -m "docs(ingress): nip.io 임시 노출 의도·치환·폐기 전제 문서화 (#121)"
```

---

## Task 6: Image Updater annotation 사전 점검 (#122 검증 전용)

**Files:**
- Read-only: `argocd/applicationset.yaml`

#122는 신규 코드 없음 — annotation이 완비됐는지만 확인(이미 머지 상태). 누락 시에만 후속 task로 분기.

- [ ] **Step 1: 필수 annotation 6종 존재 확인**

Run: `grep -cE "argocd-image-updater.argoproj.io/(image-list|app.update-strategy|app.allow-tags|write-back-method|write-back-target|git-branch)" argocd/applicationset.yaml`
Expected: `6`

- [ ] **Step 2: write-back이 git/kustomization/main인지 확인**

Run: `grep -E "write-back-method|write-back-target|git-branch" argocd/applicationset.yaml`
Expected: `git`, `kustomization`, `main`.

- [ ] **Step 3: 결과 메모 (코드 변경 없음)**

확인 결과를 런북 Phase 0 체크박스에 반영. 6종 모두 존재하면 #122는 라이브 검증만 진행. (누락 발견 시 별도 task로 annotation 추가 — 현재 기준 누락 없음.)

---

## Task 7: W5 clearance window 런북 작성

**Files:**
- Create: `docs/runbooks/W5_CLEARANCE_WINDOW.md`

- [ ] **Step 1: 런북 작성 (Phase 0~4 체크리스트)**

```markdown
# W5 진입 차단 클리어 — 실행 런북

대상: #91 #92 #120 #121 #122 / 통보 허브: synapse-shared#20
원칙: Phase 0는 무비용(사전 머지). Phase 1 진입 시 과금 ON → Phase 4 destroy로 차단.

## Phase 0 — 사전 (window 전, main 머지 완료)
- [ ] nip.io ingress 2종 + gen-nipio-selfsigned.sh + README 머지
- [ ] kafka-topics/k8s-kafka-config .terraform.lock.hcl 커밋
- [ ] Image Updater annotation 6종 확인 (`grep -c ... argocd/applicationset.yaml` == 6)
- [ ] ACM import IAM 권한 + 리전(ap-northeast-2) 사전 점검

## Phase 1 — apply + 부트스트랩 (과금 ON)
- [ ] `bash scripts/bring-up.sh` (terraform apply → ArgoCD HA+ESO+ApplicationSet, dev auto-sync)
- [ ] bastion에서 `terraform -chdir=infra/aws/dev/kafka-topics apply` (MSK TLS provider)
- [ ] `kubectl get applications -n argocd` 으로 App 등록 확인

## Phase 2 — 핵심 검증 (bastion SSM, 병렬 가능)
### #91/#92
- [ ] (team-lead) `bash scripts/verify-argocd-deploy.sh synapse-dev` → 15/15 ALL PASSED
- [ ] (team-lead) staging 수동 sync: `argocd app sync synapse-<svc>-staging` (5개)
- [ ] (team-lead) `bash scripts/verify-argocd-deploy.sh synapse-staging` → 5/5 (platform-svc 포함)
- [ ] 롤백 1회: `kubectl -n synapse-dev rollout undo deploy/<svc>` → 복구 <3분
### #120
- [ ] 토픽 생성 확인: `kafka-topics.sh --bootstrap-server <MSK_TLS> --command-config tls.properties --list`
- [ ] ACL 확인: `kafka-acls.sh --bootstrap-server <MSK_TLS> --command-config tls.properties --list`
- [ ] TLS 핸드셰이크: `openssl s_client -connect <MSK_BROKER>:9094 -servername <broker>` → 인증서 체인 확인
- [ ] produce/consume: `kafka-console-producer/consumer` 로 토픽 송수신 통과
### #122
- [ ] ECR에 새 semver 태그 푸시(5개 앱 중 대상) → Image Updater git write-back 확인
- [ ] 푸시→dev 반영 평균 시간 측정 → ≤5분
- [ ] 롤백: write-back 커밋 revert(또는 이전 태그) → 이전 이미지 복귀 확인

## Phase 3 — #121 외부 노출 (ALB 의존)
- [ ] nip.io ingress 2종 apply (cert-arn 미설정 → ALB 프로비저닝)
- [ ] `kubectl get ingress -A` 에서 공유 ALB DNS 확보
- [ ] `bash scripts/gen-nipio-selfsigned.sh <ALB_DNS>` → `CERT_ARN=...`
- [ ] ingress의 `<ALB_IP>`·`<ACM_ARN>` 치환 후 재apply
- [ ] `curl --cacert .nipio-certs/ca.crt https://argocd.<IP>.nip.io` → 200 + 체인 유효
- [ ] `curl --cacert .nipio-certs/ca.crt https://dev.<IP>.nip.io/actuator/health` → gateway 도달
- [ ] GitHub webhook ping → argocd `/api/webhook` → 200 도달

## Phase 4 — 정리
- [ ] 5 이슈 DoD 결과를 synapse-shared#20 에 코멘트 (이슈별)
- [ ] #92 해소 시 #91과 동반 클로즈
- [ ] `bash scripts/bring-up.sh --destroy` (terraform destroy, 과금 차단)
- [ ] destroy 후 `terraform -chdir=infra/aws/dev show` 빈 상태 확인
```

- [ ] **Step 2: yamllint/markdown 점검 불필요 — 커밋**

```bash
git add docs/runbooks/W5_CLEARANCE_WINDOW.md
git commit -m "docs(runbook): W5 진입 차단 클리어 실행 런북 (Phase 0~4, #91/#92/#120/#121/#122)"
```

---

## Task 8: Phase 0 PR 생성

**Files:** (없음 — PR 작업)

- [ ] **Step 1: 브랜치 push**

```bash
git push -u origin docs/w5-clearance-design
```

- [ ] **Step 2: PR 생성 (스펙·plan·Phase 0 산출물 묶음)**

```bash
gh pr create --repo team-project-final/synapse-gitops --base main \
  --title "feat(gitops): W5 진입 차단 클리어 Phase 0 — nip.io ingress + 인증서 스크립트 + 런북 (#121 외 4건 검증 준비)" \
  --body "스펙·plan(docs/superpowers) + #121 nip.io ingress 2종 + gen-nipio-selfsigned.sh + W5 clearance 런북 + kafka-topics lock 커밋. 라이브 검증(#91/#92/#120/#122)은 런북 Phase 1~4로 window에서 수행."
```

- [ ] **Step 3: PR 번호 확인 + 리뷰 요청**

Run: `gh pr view --repo team-project-final/synapse-gitops --json number,url`
Expected: PR 번호·URL 출력. 머지 후 window 진입 가능.

---

## Self-Review

**1. Spec coverage:**
- §1 범위 5이슈 → Task 1(#120 lock), Task 2·3·5(#121 ingress/README), Task 4(#121 스크립트), Task 6(#122 점검), Task 7 런북(#91/#92/#120/#122 검증 절차) ✅
- §3 Phase 구조 → Task 7 런북이 Phase 0~4 그대로 반영 ✅
- §4 #121 산출물 3개 → Task 2/3/4 ✅ (argocd-server는 NLB 서비스에 additive ALB ingress로 정정 반영)
- §5 검증 매트릭스 → Task 7 런북 Phase 2/3 체크리스트에 판정 기준(5/5, <3분, ≤5분, 200) 반영 ✅
- §6 리스크/롤백/통보 → 런북 Phase 4(shared#20 통보, destroy) + Phase 0(ACM 권한 사전점검) ✅
- §7 산출물 → File Structure 표와 일치 ✅

**2. Placeholder scan:** `<ALB_IP>`·`<ACM_ARN>`는 **의도된 런타임 치환 토큰**(README·런북에 치환 절차 명시) — 미완성 placeholder 아님. TBD/TODO 없음. 모든 코드 step에 실제 내용 포함 ✅

**3. Type consistency:** 스크립트 출력 `CERT_ARN=`·SAN 호스트(`argocd.<IP>.nip.io`/`dev.<IP>.nip.io`)·`group.name: synapse-nipio`가 ingress·README·런북에서 일관 ✅
