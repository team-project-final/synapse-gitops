# 핸드오프: 2026-06-12 세션 — #174 인증 검색 E2E 완주 (3겹 차단 해소) + destroy 대기

> **작성**: 2026-06-12 오전 · **선행**: `HANDOFF_2026-06-11.md`
> **클러스터**: ⚠️ **유지 중(과금)** — shared 24h 소크 사인오프 **오늘 17:15 KST** 예정, 직후 destroy

---

## 0. 현재 상태 (실시간)

- **17:15 KST 소크 사인오프 대기** → 직후 `bash scripts/bring-up.sh --destroy`(#178 orphan LB 선정리 실증 겸, 과금 차단).
- 소크 윈도우 중 본 세션의 재기동 내역은 **#183 코멘트로 고지 완료**(사인오프 판단자료).

## 1. 핵심 성과 — #174 인증 검색 200 E2E 완주 · close

게이트웨이 토큰 풀 플로우 검증: signup 201 → login(accessToken) → 노트 생성 200 → **검색 200 + nori 형태소 하이라이트**(`토크나이저`→`토크`+`나이저` 분해 매칭). P3 검색 0.105~0.183s.

거기까지 **3겹 차단**을 증거 기반으로 해소(전부 PR 머지):

| 겹 | 이슈/PR | 내용 |
|---|---|---|
| 1 | #196 / PR #197 | **인증 API 전면 401**: gateway SM 공개키가 06-08 시드 때 다른 키로 오염(modulus 대조로 확정) + knowledge·engagement·learning-card는 JWT_PUBLIC_KEY 미주입(ephemeral 폴백). → 4앱 전부 `synapse/{env}/platform-svc/jwt-public-key` **단일 출처** 참조. SM 값 라이브 교체 완료 |
| 2 | #198 / PR #200 | **검색 인덱싱 3중 단절**: ① search-sync 토픽(+.dlq) topics.txt 정본 누락(라이브 생성 완료) ② knowledge Redis 미설정→idempotency store가 localhost 폴백→전 레코드 DLQ행(DLQ 헤더로 확정) ③ SEARCH_AI_BASE_URL 미설정 |
| 3 | #199 (**OPEN, 팀 결정**) | **dev/staging이 같은 MSK·같은 컨슈머 그룹 공유** → 파티션 환경교차 분산, dev 이벤트 2/3이 staging으로. 전 토픽 구조적 문제. Day5 데모 전 일독 필수 |

## 2. 그 외 마감 (이 세션)

| 항목 | 결과 |
|---|---|
| **#194 close** | SynapseHighMemory 12건 = **이중합산 오탐**(`container!=""` 부재, 사용률 2배). 룰 수정 + 진성 압박인 platform-svc만 limit 768Mi 상향(12h P95 96.7% 실측, PR #201). 적용 후 신규 파드 52~53%. 잔여 PodDown/TargetDown은 owner 4이슈로 기분배 |
| **#164 close** | learning-card dev 1/1·staging 2/2 RESTARTS 0(18h+) 재확인. 잔여 readiness 401은 learning-svc#74 |
| **#182 진행** | 진단 정정: IAM 인라인 정책은 현존(managed만 조회 시 누락 오판). 실원인 = **cloud-init IAM 전파 레이스** → depends_on+재시도 루프(PR #202). **ECR 8종 standalone 스택**(`infra/aws/ecr/`, 자체 state·prevent_destroy) import+apply 완료 — destroy에서 이미지 보존. 잔여 = 다음 윈도우 무인 완주 검증 |
| **#165 코멘트** | 결정 요청: dev=IU SHA 핀 / staging=검증 SHA 수동 승격 / semver=릴리스 스냅샷 제안. knowledge staging 오버레이에 `dev-latest` 잔존 |

## 3. 머지된 PR

| PR | 내용 |
|----|------|
| #197 | JWT 공개키 단일출처화(4앱) — Closes #196 |
| #200 | 검색 파이프라인 3중 단절 해소 — Closes #198 |
| #201 | HighMemory 오탐 수정 + platform-svc 768Mi — Refs #194 |
| #202 | bastion 레이스 방어 + ECR standalone IaC — Refs #182 |

## 4. OPEN / 잔여

| 항목 | 상태 |
|---|---|
| **destroy(#183·#178)** | **17:15 사인오프 후 실행** — 유일한 당일 잔여 |
| **#199** | 팀 결정(컨슈머그룹 env 분리 A/B/C안). 데모 시 staging knowledge가 dev 이벤트 소비 가능 — 임시책은 scale-down이나 selfHeal이 원복시키는 점 주의 |
| **#165** | 팀 결정 대기(코멘트로 제안 제출) |
| **#182** | 다음 윈도우 bring-up 무인 완주 검증만 남음 |
| **shared#31** | shared 봇토큰 전환 선행 — 변동 없음 |
| **앱 actuator 4건** | platform#101·engagement#45·knowledge#82·learning#85 (owner) — 해소 시 firing=Watchdog 자동 도달 |

## 4.5 다음 윈도우 체크리스트 (저녁 갱신)

> destroy 완료(63리소스+orphan SG·EBS, 과금 0 검증, #183 close). 선정리 갭 3건 수정(PR #209).
> gateway#7은 **코드·이미지·gitops 핀까지 전부 staged** — PR #8 머지, ECR `synapse/gateway:5e2c73b3…` push 확인(ECR은 standalone 스택이라 destroy에서 보존), dev overlay `newTag=5e2c73b3…`(IU 봇). gitops 측 추가 작업 없음.

bring-up 후 순서대로:

1. **#182 무인 완주 검증** — bastion cloud-init이 ArgoCD 부트스트랩까지 자동 완료되는지(레이스 수정 PR #202 실증). 통과 시 #182 close.
2. **gateway#7 4단계** (정확한 명령은 [gateway#7 코멘트](https://github.com/team-project-final/synapse-gateway/issues/7)에 있음):
   1. gateway deploy 이미지 == `5e2c73b3…`
   2. 파드 내부 `GET /actuator/prometheus` → 200
   3. Prometheus targets에서 gateway `health=up`
   4. TargetDown/SynapsePodDown firing 없음 → **firing=Watchdog만** 확인
   - 통과 시 gateway#7 close. (선행: 앱 actuator owner 4건 중 knowledge·learning은 dev 반영됨, engagement#45·platform#101 상태 확인)
3. **destroy 갭 수정 실증(PR #209)** — 윈도우 종료 teardown에서 LB svc 삭제·k8s-* SG 정리·CSI EBS reap 동작 확인.

## 5. 다음 세션 함정 메모

- 게이트웨이 경로는 `/api/{service}/api/v1/...`(stripPrefix 2). 공개 경로는 `/api/platform/api/v1/auth/{signup,login,refresh}` 등.
- Git Bash curl은 한국어 본문을 CP949로 보냄 → python으로 UTF-8 파일 생성 후 `--data-binary @file`.
- aws CLI `file://`는 MSYS 경로 불인식 → `cygpath -m` 변환.
- 이 레포는 **머지커밋 전용**(squash 거부) + 체크 통과 후 머지 가능(auto-merge 미허용).
- staging knowledge 2파드는 구설정(Redis/JWT 머지분 미적용) — 다음 재기동 시 자동 적용.
