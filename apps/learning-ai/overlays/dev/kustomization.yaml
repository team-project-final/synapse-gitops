apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
namespace: synapse-dev
patches:
  - target:
      kind: Deployment
      name: learning-ai
    patch: |
      - op: replace
        path: /spec/replicas
        value: 1
  - target:
      kind: ConfigMap
      name: learning-ai-config
    patch: |
      - op: replace
        path: /data/LOG_LEVEL
        value: "DEBUG"
      - op: add
        path: /data/DATABASE_HOST
        value: "synapse-dev-postgres.c7emuq20mhyy.ap-northeast-2.rds.amazonaws.com"
      - op: add
        path: /data/DATABASE_PORT
        value: "5432"
      - op: add
        path: /data/DATABASE_NAME
        value: "synapse_ai"
      - op: add
        path: /data/LEARNING_AI_DATABASE_URL
        value: "postgresql+asyncpg://synapse_admin@synapse-dev-postgres.c7emuq20mhyy.ap-northeast-2.rds.amazonaws.com:5432/synapse_ai"
      - op: add
        path: /data/LEARNING_AI_KAFKA_ENABLED
        value: "true"
      - op: add
        path: /data/LEARNING_AI_SCHEMA_REGISTRY_URL
        value: "http://schema-registry:8081"
      # dev MSK는 TLS 전용 리스너(client_broker=TLS, 9094). aiokafka 클라이언트가
      # SSL로 핸드셰이크하도록 지정(미설정 시 PLAINTEXT 기본→TLS 브로커 연결 실패).
      # MSK 인증서는 Amazon Trust Services 발급→JDK/Python 기본 truststore로 충분.
      - op: add
        path: /data/LEARNING_AI_KAFKA_SECURITY_PROTOCOL
        value: "SSL"
      # Kafka 토픽 환경 프리픽스 (#199): dev·staging 공유 MSK 환경 교차 격리 — 앱이 모든 발행/구독 토픽명 앞에 붙인다.
      - op: add
        path: /data/LEARNING_AI_KAFKA_TOPIC_PREFIX
        value: "dev."
  - target:
      kind: ExternalSecret
      name: learning-ai-external-secret
    patch: |
      - op: replace
        path: /spec/secretStoreRef/name
        value: aws-secrets-manager
images:
  - name: ghcr.io/team-project-final/synapse-learning-ai
    newName: 963773969059.dkr.ecr.ap-northeast-2.amazonaws.com/synapse/learning-ai
    # image-updater A안: semver 베이스라인 핀 (was 3774e2e6…SHA).
    # ECR 1.0.0은 #144(aiokafka ssl_context) 수정 이미지 3774e2e6(앱팀 PR #63 머지·deploy bump됨)에 재태그할 것.
    # #144 close는 다음 윈도우 dev 파드 CrashLoop→Running 확인 후.
    newTag: 559791c143437c37912d00659a8433901b539033
